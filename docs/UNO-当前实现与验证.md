## 2026-09-22：稳定性深化提交前复核

按照 React 请求去重、异步边界与状态归属规则复核现有差异，并补齐取消回归：进入等待前已取消的调用仍接管底层 Promise 的迟到拒绝；读取 403 错误正文期间也响应取消，不重发业务操作。未扩展真实知识写入或运行服务范围。

提交前重新运行：后端 835 项，823 通过、12 跳过；前端 43 个文件、281 项全部通过；Python 28 项，27 通过、1 跳过；TypeScript 与生产构建通过。主入口约 579.53 kB、gzip 约 180.75 kB，仍有超过 500 kB 的提示。浏览器证据沿用下节合成环境验收，本次没有重启正式服务或新增真实模型请求。下节列出的未完成项仍然保留，不因提交或推送而视为完成。

## 2026-09-22：稳定性与性能深化实施

依据 [稳定性与性能深化修改方案](UNO-稳定性与性能深化修改方案.md)，完成 F01—F14 的主要防护与资源控制切片。下表区分已实现与尚未完成的要求；不表示原方案全部验收条件已经满足。业务协议、模型预算、Gateway 写入边界与真实知识内容没有调整。

| 项目 | 当前实现 | 剩余工作／验证边界 |
| --- | --- | --- |
| F01 元数据 | 仅文件确实不存在时初始化；JSON、结构与读取异常返回明确错误；失败后禁止写回，写前核对原文件摘要，保留损坏原字节 | 最近有效快照的只读展示、备份恢复及诊断导出界面尚未实现；跨进程写竞争仍需专门测试 |
| F02 实例 | 登记结构与目录可用性分开；逐项报告可用、离线或清单损坏；离线非当前实例可移出登记，不删除目录；激活和改名仍验证清单 | 当前实例在启动前已离线的完整启动救援、重新定位目录入口尚未实现 |
| F03 切库 | 切换成功立即采用已确认 ID；资源独立刷新；请求携带库身份，旧代次响应及迟到 JSON 被拒绝；切换 15 秒未结算时先核对后台；身份不确定期间阻止写操作；订阅失败保留告警并拒绝未同步写入 | 真实多标签页竞争、四种资源分别失败、切库响应丢失的完整浏览器矩阵尚未做完；订阅同步失败后的恢复仍使用重切／重启核对 |
| F04 卡片 | 扫描报告 complete／partial／unavailable 及文件级诊断；图谱和卡片目录读取可返回正常对象，界面显示缺口；正式读写校验保持严格，不把坏卡视为不存在；关系解析共用含退役卡的快照 | 降级检索、上次完整快照、损坏清单的交互式修复入口尚未实现；领域定义损坏仍可能阻断对应领域读取 |
| F05 事务 | 锁记录进程身份、时间与事务 ID；属主建立失败收尾；缺失／损坏属主保守拒绝并提供检查结果；收据已存在时，清理失败返回已提交且清理待处理；同幂等键仍返回原收据 | 无属主锁的人工安全恢复界面、全部文件替换窗口及真实 PID 复用故障矩阵未完成；不会按年龄自动删锁 |
| F06 上传 | 完整暂存后核对长度、摘要，以同目录硬链接排他发布；失败先中止并等待写流，再清理自身临时文件；批次有 120 秒期限及停止入口；不再启动后续批次，未确认结果不宣称未保存 | 不支持硬链接的文件系统安全拒绝，不降级为非原子复制；磁盘满、进程强杀、残留清理工具及逐文件导出未完成；重试利用既有同名摘要核对 |
| F07 预处理 | 准备接口说明保存与编译上限；超过 50 MiB 的原件不可在新编译选择中选中；子进程 120 秒期限、64 KiB stderr 尾部、终止后等待 close；EPUB／DOCX 共用 20,000 条目及 200 MiB 展开／读取预算；图片摘要 Map 和 PDF xref 复用 | 子进程强杀失败、峰值内存、实际退出时间、大型 PDF 库内部解析资源上限仍需压力验证；不以 stdout 上限代替总内存边界 |
| F08 安全会话 | 令牌获取 10 秒期限；等待者可独立取消；超时代次失效，晚到旧令牌不覆盖新值；仅明确 LOCAL_TOKEN_INVALID 的 403 可重发；普通读取默认 15 秒期限，覆盖正文解码 | 尚未对真实服务重启和真实网络半开连接完成浏览器矩阵；外发写操作没有增加通用自动重试 |
| F09 浏览器存储 | 偏好按所属库完成读取后才保存；localStorage／sessionStorage 失败回退页面内存并提示刷新限制；待提交请求保留相同幂等 ID，结算不因存储失败重建任务 | A／B 不同收藏连续十次切换的真实挂载验收及跨刷新服务器核对入口仍待补齐；内存回退不承诺跨刷新持久化 |
| F10 异步面板 | 四个按需加载面板使用局部错误边界，提供可关闭的等待、失败和重试；同源 JS 块错误可用新查询参数真正重新加载；外层页面保留 | 实测提示词块 404→恢复；其余三个块、嵌套依赖／CSS 失败需继续验证。无法恢复时提示复制输入后刷新，不声称所有模块故障都可原地修复 |
| F11 轮询 | 三条轮询改为前次结算后再调度，错误退避，后台页／离线降频；相同库和会话的认知请求共享在途 Promise；同步失败不清空最后有效认知状态 | 与更新更快的 SSE 快照之间尚未全面接入后端单调版本；长时间多标签页、终态乱序及每种资源的过期状态提示仍需完善 |
| F12 SSE | 每连接队列至多 96 帧／256 KiB，处理 write=false 和 drain；超限只断开慢连接；历史全局 256 会话／4 MiB、空闲 5 分钟回收；SSE id 使用代次和序号，重连同步快照 | 内存响应替身已验；真实 socket 长时背压、服务重启及高频重连仍需压力验证 |
| F13 检索 | 相同快照内传递关系目标；按正文摘要复用每文档词频；单卡变化只重分词该卡；缓存保存失败返回内存结果和诊断，30 秒后新查询可重试保存 | 仍枚举全量文件版本，变更时仍组装文档、统计全局词频并序列化整个缓存；尚未做 1k／5k／10k 基准、worker 或完整文件级增量索引 |
| F14 请求记录 | 输入与输出分开保存，不变输入只写一次；增量保存按至少 2 秒且新增 4,096 字符合并，终态立即保存；淘汰取消排队，旧格式可读；单文件 8 MiB、记录目录约 64 MiB 预算与失败提示 | 检查点仍使用同步文件 I/O，目录预算仍逐项查大小；没有慢盘基准、持续输出峰值内存或完整异步单写队列验收；诊断记录不能替代正式账本与原始修复响应 |

### 本地验证

- `npm test`：835 项，823 通过、12 跳过、0 失败。
- `npm run test:web`：43 个文件，279 项全部通过。
- `python -B -m unittest discover -s tests -p 'test_uno*preprocess*.py'`：28 项，27 通过、1 跳过。
- `npm run build`：TypeScript 与生产构建通过；主入口约 579.47 kB，gzip 约 180.73 kB。保留超过 500 kB 的构建提示，不宣称主包体积缩小。
- 新增 `storage-resilience.test.mjs`、`resource-resilience.test.mjs`、`resilience.test.ts` 与 `test_uno_resource_preprocess.py`，覆盖损坏元数据不覆盖、离线实例、订阅异常、坏卡隔离、锁属主失败、提交后清理、SSE 背压和容量、分词复用、输入单次保存、安全令牌取消／超时／迟到、迟到 JSON、未知库身份禁止写入、存储失败请求 ID 保留、轮询不重叠与退避、ZIP 预算。
- Windows 上传回归包含 1,400 个小文件和同名文件行为，实际使用本机文件系统。该结果不等于已验证所有网络盘或非 NTFS 文件系统。
- 合成 20 卡库中，初次索引分词 20 个文档，单卡变更后的分词数为 1；模拟缓存写失败后仍能检索。100,000 字符输入在开始加十次保存中仅写一份输入文件。SSE 连续 1,000 帧下，慢响应只接收首次 write 后进入队列并被有界断开，正常响应收到 1,000 帧。这些是限定场景的计算／调用次数证据，不是整机延迟或吞吐提升百分比。

### 浏览器故障验收

使用当前 `web/dist` 和本机临时合成 HTTP 服务 `127.0.0.1:65347`，全部接口为合成数据，不代理正式服务、不访问真实知识库。通过浏览器工具实际渲染及点击验证：存储写入拒绝时页面继续可用；A 切 B 后图谱 503 时身份仍为 B，库选择器、卡片入口和重试保留；提示词 JS 块 404 时只显示局部错误，恢复资源后点击重试实际重新加载并打开面板；390×844 下故障提示、重试和关闭可读可操作。新增警告放入独立状态区，避免挤压窄屏库选择器。控制台中存在故障注入预期的动态模块加载错误；未观察到额外框架崩溃。语音接口为合成环境未支持项，不将其计为正式语音验证。

本机临时证据：`%TEMP%/uno-resilience-backend-tests.log`、`uno-resilience-web-tests.log`、`uno-resilience-build.log`、`uno-resilience-python-tests.log`；截图位于 `%TEMP%/uno-resilience-qa/`，其中 `04-narrow-fallback.png` 与 `05-final-panel-recovered.png` 分别记录窄屏错误和重试恢复。临时证据不作为程序分发内容。

### 运行与撤回边界

本轮未调用真实模型、迁移或清理知识体、改写正式任务、重启正式服务，也没有提交或推送 Git。上述结果只证明本地源码与合成验证，不证明当前服务进程已加载。需要上线时，应先核对活动任务并使用现有安全停止流程，再独立验证服务身份与新行为。

撤回仅涉及程序差异，保留真实材料、元数据原件、事务日志、锁诊断、收据和浏览器待提交 ID。请求查看器的新记录引用 `.input` 文件，若撤回其他程序，应保留兼容新旧格式的读取器；不能让旧读取器直接接管新记录。稀疏缓存可在后续明确操作中重建，Markdown 始终是语义事实源。未完成项按上表继续实施，不用本次通过数覆盖其验收缺口。

## 2026-09-22：运行一致性、安全与性能收口

任务状态保存增加版本比较与原子替换，过期执行快照不能覆盖较新的暂停、结束或批次停止标记；异步执行统一托管并在异常、取消和版本冲突后结算，读取任务不再隐式改写状态，启动时单独恢复中断任务并隔离损坏记录。自定义模型端点的凭据按端点作用域保存，设置文件落盘失败时不会让新端点密钥污染仍在使用的旧端点。上传改为流式落盘、逐文件限额、总量限额与同知识库并发限制，不再把大批文件整体读入内存；知识快照和图谱索引按版本复用，减少同一请求内的重复全库扫描。对话历史合并后的总量限制、跨会话顺序和前端稳定消息标识已统一，设置、卡片、知识工作台和提示词检查器改为按需加载。局域网可信主机校验同时支持带端口请求头与显式 authority。

全量后端测试 824 项：812 通过、12 跳过、0 失败；前端 42 个测试文件、272 项全部通过；Python 预处理与 EPUB 测试 32 通过、1 跳过；TypeScript 与生产构建通过。根目录、Web 与宿主三个依赖树的 `npm audit` 均为 0 个已知漏洞。生产构建的主入口约 572.21 kB、gzip 约 178.18 kB，设置、卡片、知识工作台和提示词检查器已拆为独立异步分块；Vite 仍提示主入口超过 500 kB，属于后续可继续拆分的性能债。本轮没有调用真实模型、改写知识实例或重启正式服务；源码、测试和构建通过不代表当前运行进程已经加载这些改动。

## 2026-09-21：按模型能力冻结编译与建构资源

新建编译与建构任务新增 `workflow_limits=provider-aware-v1`，按创建时的供应商、精确模型、登记上下文和单次输出能力冻结输入字符与分阶段输出上限。百万级上下文模型使用 90,000 字符原文单元和 90,000 字符其余上下文，256K 模型使用 72,000/72,000；旧任务缺少快照时继续使用原 60,000/60,000。输出能力足够时，制卡、局部修复和审核分别可使用 98,304、49,152、16,384 tokens，但所有环节都取模型登记上限与阶段上限的较小值。预处理和不可变图书目录同步记录真实分段上限，读取端按目录验证，不用截断来假装已完整交付。

模型目录现在显式记录 `context` 与 `maxOutput`，并覆盖 DeepSeek、Kimi 开放平台、Kimi Code Plan、GLM、阿里云百炼、火山方舟、百度千帆、腾讯混元、硅基流动及自定义 OpenAI 兼容入口。火山方舟、千帆与混元使用独立固定端点和凭据槽，模型 ID 由用户按所属账号与控制台填写；未登记型号保持保守资源上限，不猜测图像、思考或窗口能力。Kimi Code 的 `k3`、`k3-256k` 和 `kimi-for-coding` 支持关闭思考及 low/high/max；HighSpeed 保留固定思考，编译与建构不发送虚构强度。原生 `kimi-coding` 路由在新任务建立前同步模型覆盖，修正安装目录中旧的上下文、输出与思考能力。固定思考且没有档位的模型使用供应商默认；自定义未知模型保持保守输入上限和输出默认，不根据品牌名猜测能力。

定向后端回归 147/147、Python 预处理 7/7、TypeScript 与生产构建通过；新增三家兼容端点后，模型适配与连接测试专项 33/33、图书任务与存储专项 55/55、Python 预处理边界 25 通过且 1 跳过。新增测试覆盖 90,000 字符原文完整分段、98,304 输出上限、旧任务回退常量、Kimi Code 原生能力覆盖、固定思考参数省略以及三家新增端点的独立路由与保守资源回退；模型目录中的兼容旧 ID 也会按当前规范 ID 正确确认，不再造成假阴性。后端全量运行 816 项时记录 791 通过、13 失败、12 跳过；其中任务创建守卫与模型连接目录断言已在随后定向重跑中通过，其余失败集中在仓库已有的旧 Gateway、凭据夹具和生命周期断言，因此本轮不声明后端全量通过。前端全量 265/267，两个失败来自同一工作区并行存在的对话加载提示文案改动；本次模型设置与生产构建通过。正式 3093 已在无运行知识任务时安全重载资源上限部分，健康接口返回 `uno_provider_aware_limits=provider-aware-v1`。新增火山方舟、千帆与混元入口完成后，健康接口检测到新的活动知识任务，因此没有中断该任务再次重载；这三个入口需待任务自然结束并安全重启宿主后才会在后端设置接口完整生效。设置页只展示后台实际公布的供应商，因而重载前不会出现“前端可选、后端拒绝保存”的假入口。Edge/Playwright 在 1440×1000 与 390×844 只读核对当前 `kimi-for-coding` 显示“支持开关思考、上下文 1M、输出 128K”，无控制台错误且窄屏无横向溢出。本轮没有调用真实模型或改写知识实例。

## 2026-09-19：随机焦点与全库关系候选检索

新建“发现联系”与允许新增关系的“综合整理”任务改用 `random-focus-semantic-retrieval-v2`。焦点卡不再按完全孤立、知识岛或主图位置排序，而是用任务 ID 作为可恢复随机种子，从当前范围内尚未检查的有效卡片中无放回抽取；指定卡片模式只约束焦点范围。焦点冻结后，`all-cards-integration-candidates-v1` 从全部有效知识卡中按共享来源、显式互指、语义单元、候选关联、类型兼容与既有路径惩罚召回最多五个端点，领域和拓扑位置都不限制端点池。拓扑数据继续计算，但只作为诊断和以后独立治理的依据。

宿主仍直接冻结关系工作包，因此正常一轮保持作者与独立审核两次模型请求，不增加选卡请求。没有达到召回闸门的端点时记录 `no-selection`，不调用模型，也不宣称焦点必然独立。同一焦点若审核或证据未通过，只更换一次尚未尝试的端点；成功关系实际涉及的只读端点也记为本任务已比较，避免马上反向重复检查同一对。历史 `incremental-relation-weaving-v1` 任务继续按孤立卡后知识岛的原协议恢复，不自动改签。

验证：策略、关系发现、恢复与宿主专项 55/55 通过；全库召回测试包含“无关主图节点”与“语义相关的非主图节点”，确认排序由关系检索信号决定而不是主图优先。测试使用临时合成知识库，没有调用真实模型或改写正式知识实例。

## 2026-09-19：建构响应恢复与关系编织有界续行

建构作者、审核、局部修复和复核响应现统一使用 `construction-response-recovery-v1`。宿主先确定性补齐无歧义的局部结构，例如审核明确批准但遗漏空 `issues`；其余格式错误最多追加一次只含失败响应、校验错误和局部 Schema 的恢复请求，不重复发送卡片、来源、候选目录或历史。恢复后仍须经过原检查器；再次失败时原始响应、错误、阶段和请求账本保留，显式继续只恢复这个阶段。准备与健康接口同时声明该契约，旧服务不会让新版界面静默启动任务。

关系编织改为审核通过后才暂存作者变更。审核拒绝关系、修复后删除不可靠关系或结算为延期时，不留下待发布草稿；同一结构焦点下一轮排除已经尝试的端点，最多换一组端点再次判断。第二组仍无可靠关系时登记为 `deferred-exhausted`，保留待办并继续其他孤立卡或知识岛。界面分别显示“保留独立”“更换端点后继续”“两组端点均未通过，保留待办”，并显示本焦点的有界尝试轮次；不会把一次候选失败误报成整项任务自然完成。

专项后端回归 54/54、前端全量 247/247、TypeScript 与生产构建通过，构建只保留既有主包体积提示。后端全量 800 项中 775 通过、13 个既有失败、12 跳过；失败仍集中在凭据夹具、知识质量旧断言、设置连接和既有生命周期测试，本轮新增建构恢复、换端点续行和结算显示测试均通过，因此不声明后端全量通过。正式 3093 上仍有一项真实编译任务运行，安全停止边界不允许此时重载；只读浏览器验收确认新版前端会因旧服务缺少两个新能力标识而禁用开始按钮并给出明确提示，已有建构结算仍可查看，控制台无警告或错误。本轮没有调用付费模型，也没有读取或写入真实知识实例。

## 2026-09-19：关系编织修复端点与原地恢复

真实关系编织任务 `f4107f67-2472-48c9-98cd-53e38cb4decf` 已结算 14 轮，在第 15 轮因关系说明把 `supplement` 的方向写反而进入局部修复；修复响应已经把说明改成“当前卡补充目标卡”，但旧宿主把修复包缩成仅含被拒绝卡，随后又以这个缩小范围检查关系目标，错误报出“关系另一端也必须属于策略已选择范围”。该任务共记录 32 次模型请求和 13 份正式写入收据。用户另建任务 `6d3c1bb1-f368-451e-b2d0-2e03c811ba75` 后，第一轮保留孤立卡，第二轮再次命中同一卡和同一关系方向问题；作者、审核、修复三次请求完成后被同一旧宿主错误中断，因此任务共记录 5 次请求、0 份正式写入收据。

关系审核修复现使用 `construction-repair-endpoint-scope-v1`：模型只返回被拒绝卡，宿主却使用原冻结工作包作为允许端点集合，并继续把其他卡作为只读比较材料交付给修复与复核。作者和审核提示同时提前给出“当前卡指向目标卡”的七种关系方向，减少因 `supplement` 主语写反而增加修复与复核请求的概率。若审核后的有效局部修复已经持久化但停在旧端点预检，恢复计划显示“应用已保存修复并复核”；显式继续不会再次调用作者、首次审核或修复，只在宿主预检通过后为当前轮发出一次独立复核，随后按任务原有的连续处理设置运行。任务不会因服务重载自动继续。

建构与恢复专项 56/56、建构面板 25/25、TypeScript 与生产构建通过，构建只保留既有主包体积提示。正式 3093 已在无执行任务时安全重载；健康接口与准备接口均返回 `construction_repair_scope=construction-repair-endpoint-scope-v1`。重载后新任务仍保持 `failed/repairing`、5 次请求、0 份收据，恢复计划已变为可安全继续；本轮没有点击继续、没有新增模型请求，也没有写入知识卡。

## 2026-09-19：编译停点安全出口与结算页收敛

启用 `compile-isolation-v1` 的 v3 编译在问题卡达到自动修订上限后，不再把“放弃未通过候选”作为唯一可完成主线的出口。`book-pause-resolution-v2` 新增 `quarantine-candidates`：保留候选、问题、来源版本和审核记录，把当前单元按待修复结算后继续主线；放弃与延期仍是明确的次要选择。关系修复若改写非关系字段或引入未核对端点，现标记为 `UNIT_REPAIR_SCOPE_VIOLATION` 并作为关系待修复项留池，不再暂停整条主线。

结算页先显示阅读单元、待修复项和可执行下一步；阅读单元记录、大量已保存卡片、模型用量与合并后的原件提醒在主线结束后默认折叠。停点卡只突出建议动作，选项差异按需展开，并在同一区域提供“暂不处理，结束任务”。未组织池读取期间显示“正在读取”，不再短暂显示为 0 项或空池。结束任务的顶部摘要缩短为成果与待办保留状态，内部长结算文本不再占据首屏。

定向编译回归 85/85、前端全量 239/239、TypeScript 与生产构建通过。全量后端为 780 项：755 通过、13 个既有基线失败、12 跳过；失败仍集中在旧导入、凭据模拟和生命周期断言，不属于本轮停点与结算改动。正式 3093 在无运行任务时安全重载，健康接口返回 `uno_book_pause_resolution=book-pause-resolution-v2`。真实结束任务只读验收显示 17/20 个阅读单元完成、3 个隔离待修复单元、15 项编译待修复及 1 张正式无领域卡；打开未组织池先显示读取状态，随后显示合计 16 项，页面无控制台警告或错误。本轮没有发起模型请求，也没有改动任务、候选或知识卡。

## 2026-09-19：编译结算统计口径

编译结果页不再把不同维度的数字并列为“已处理／延期／待处理／待修复”。阅读单元统一显示为“已完成、延期、隔离待修复、未开始、总数”；未组织池另列问题项数量，并明确“项”指候选卡片、关系或响应问题，不是阅读单元。该阶段的真实任务快照显示为：已完成 16 个、延期 1 个、隔离待修复 3 个、未开始 0 个，共 20 个阅读单元；未组织池另有 17 项待修复，来自上述 3 个隔离阅读单元。两组数据口径不同但可对账；任务后续处理后的现行状态见上一节。

`partial/done` 不再显示“本次编译结束”。状态改为“部分完成／主线执行已结束”，任务摘要改为“本轮执行已结束，整本编译未完成”；只有 `completed/done` 才显示“全书编译完成”。新结算直接保存新文案，已有任务通过只读公开投影替换旧的完成式开头，不改任务文件、请求账本或知识内容。

验证：编译入口回归 18/18，前端全量 236/236，TypeScript 与生产构建通过，构建仅保留既有主包体积提示。正式 3093 已安全重载；真实接口仍为 `partial/done`、70 次请求、19 份收据和 17 个隔离问题。Browser plugin 不可用，使用本机 Edge / Playwright 在 1440×1000 与 390×844 下核对真实任务：新状态、两层计数说明和 16+1+3+0=20 均可见，旧完成文案不可见，阅读单元记录可折叠后重新展开，无框架错误、控制台错误或警告。本轮没有调用模型、处理修复项或写入知识。

## 2026-09-19：关系修复的非关系字段投影

真实编译任务 `143a04bb-2a9d-455e-ba03-85132e724cd9` 在“第十一章 中国土地管理制度的历史演进”进行关系修复时，模型按审核要求把两条 `supplement` 关系调整为从总体命题卡 `land-institution-stability-development-functions` 指向两个具体机制卡；关系端点、方向与 `basis=source` 均满足待修复问题。返回卡片的唯一额外差异是摘要把“因此土地问题”写成“因此，土地问题”。旧宿主对移除 `relations` 后的整个对象做序列化哈希比较，因此把这一处标点漂移视为越权正文改写，拒绝了整次合法关系修复并将单元延期。

关系修复现使用 `relation-scope-projection-v1`：先按对象语义比较非关系字段，避免对象键顺序造成误判；若唯一差异只是 `summary` 的 Unicode 空白或标点，宿主恢复原摘要，只采用返回的 `relations`，记录投影事件后继续独立关系复核。正文、标题、类型、领域、来源或其他实质字段有变化时仍拒绝，不把关系修复变成正文改写入口。原始模型响应继续保留，实际进入复核的是恢复原非关系字段后的候选。

验证覆盖标点漂移恢复、关系复核与发布，以及正文越权仍被拒绝；关系／隔离／恢复相关 81/81，编译入口与契约相关 41/41，共 122/122 通过，生产构建通过并仅保留既有主包体积提示。正式 3093 在真实任务离开运行态后安全重载，健康接口返回 `uno_relation_scope_projection=relation-scope-projection-v1`。重载前后任务保持 `partial/done`、70 次请求和 19 份收据；本轮共处理 16/20 个可读单元并保存或丰富 106 张卡片。17 个隔离问题保留在未组织池；本次关系拒绝所在单元则连同 6 张候选、审核问题和原修复响应保存在原任务的延期检查点，不属于这 17 个单项修复记录。它不会因服务重载自动重试或写入知识，本次也没有发起任何额外模型请求。

## 2026-09-19：运行中编译的安全停止边界

真实任务 `143a04bb-2a9d-455e-ba03-85132e724cd9` 的第 10 个单元请求于 09:07:54 发出，09:08:13 本地 UNO 服务被重启；请求因此保留为零返回的运行中记录，服务启动恢复把任务安全暂停。该现象不是输入 50,509 展示字符触发的上下文或输出上限错误，而是服务停止入口此前只核对进程身份，没有核对或锁住正在执行的知识任务。此前 6 个已处理单元、3 个待修复单元、36 张已保存卡片和 8 份 Gateway 收据均保留；中断请求在供应商账本中按已发但用量未知计一次，不计作成果。

停止流程现在使用原子的运行态封闸。`POST /api/runtime/prepare-stop` 只有在没有正在创建或执行的编译／建构任务时才进入停止准备态；进入后，新建、继续和重试入口均拒绝启动，直到进程停止或显式撤销准备态。`stop-nexogenesis.ps1` 先核对工作区身份和实际进程，再取得本地 CSRF 令牌并建立该边界；检测到活动任务、接口异常或无法证明安全时一律不停止，也不提供绕过活动任务的强制参数。首次部署到旧服务时只允许以 `/api/uno/prepare` 证明最近任务中没有运行项后停止；新版服务不再从原子接口错误降级到该兼容路径。进程失联留下的调用记录会标记为 `interrupted`，注明未收到响应不计入成果，而不是永久保留为 `running`。

验证：停止边界、任务生命周期与 Windows 启停专项 16/16；前端全量 235/235；TypeScript 与生产构建通过，保留既有主包体积提示。后端全量 773 项中 748 通过、13 个既有失败、12 跳过，失败仍集中在已删除旧接口、供应商凭据测试夹具、设置连接和既有工作生命周期断言，本次新增停止边界与中断归档测试均通过，因此不声明后端全量通过。正式 3093 已从初始 PID 12152 重载为最终 PID 25544，健康接口返回 `safe_runtime_stop=1`，实际准备／撤销时 `operations.stop_prepared` 正确切换，最终接口同时返回 `pending_mutations=0`。真实任务文件 SHA-256 重载前后保持 `AE9022A5E4C0922DC66F6752F564C95FB098EF97386F0C3CFE76A639F321D517`，仍为 paused、36 次请求、8 份收据，恢复计划为“从当前检查点继续”；没有点击继续、调用模型或写入知识。Codex 内置浏览器只读确认页面显示“已暂停”“上次执行已中断”和可用的“从当前检查点继续”，无控制台错误和横向溢出；Browser plugin 与项目本地 Playwright 包不可用。

## 2026-09-19：建构图书来源交付与预检局部恢复

真实任务 `e47abf10-e5a6-4649-a6ab-fb8471af9c6b` 显式继续后，收敛后的策略请求以 23,757 字符完整返回 12 张选卡和 2 个工作包。第一个作者请求虽携带了 6 张完整卡片，但图书来源交付为 `source_chars=0`：历史 `03-Archive/books/.../units/...` 引用在读取正文时没有解析到现行 Buffer 保留单元，现行 `05-Buffer/books/.../units/...` 又误用了只支持旧 Buffer 范围的读取器。因此 4 个实际存在且版本有效的图书单元都被标记为 `unavailable`。模型随后错误输出 `basis=source` 关系，宿主正确拒绝，但旧执行路径把这个可局部修复问题直接升级为整批失败。

建构来源读取现统一经图书单元校验器获取正文，同时支持现行 Buffer 引用和历史 Archive 引用的兼容定位。上述真实工作包的 4 个引用已只读核对，可分别交付 4,912、22,022、11,049 和 29,325 Unicode 字符。作者提示另明确规定：对应来源标记 `unavailable` 或没有 `text` 时禁止输出 `basis=source`；只依据两端卡片时只能提出有边界的 `navigation` 关系。

作者结论在任何暂存前现先对整个工作包执行确定性预检，避免后面问题卡失败时留下前面卡的半批草稿。预检问题只将被点名卡片与具体问题送入一次局部修复，修复结果合并回原作者结论后重新预检；如仍无效，该卡确定性延期而不阻塞同批其他卡。这次预检修复会消耗本工作包唯一一次修复额度，后续独立审核不会再开第二轮修复。

合成宿主回归已覆盖现行 Buffer 图书单元、历史 Archive 单元兼容定位，以及“来源未交付却生成 source 关系”的局部修复与 Gateway 发布。建构相关回归 91/91、前端 235/235 及 TypeScript 与生产构建通过，构建仅保留既有大包提示。后端全量 772 项中 747 通过、13 失败、12 跳过；失败仍集中在已删除的 `commitBuffer` 旧测试、未跟随 `credentials.describe` 的凭据夹具、设置并发及既有生命周期断言，不宣称后端全量通过。正式 3093 已重载为新进程，工作区身份与 `strategy-driven-v2` 健康标识一致。真实任务已由用户结束，仍保持 `ended`，调用记录仍为 3、草稿和发布收据均为 0；本次没有恢复它、没有追加模型请求，也没有写入正式知识。

## 2026-09-19：建构第一步输入收敛与截断恢复

真实任务 `e47abf10-e5a6-4649-a6ab-fb8471af9c6b` 的策略请求携带 79 张候选，候选目录 JSON 为 55,444 字符，供应商实际输入共 59,761 个展示字符。该任务没有填写额外说明，旧实现却让空的 `construction_query` 覆盖了由“联系建构”侧重生成的任务目标，候选召回因此没有按目标排序。`deepseek-flash` 在高强度推理中耗尽 8,192 输出 tokens，未返回可见 JSON，任务停在 `strategy_pending / MODEL_OUTPUT_TRUNCATED`；没有进入作者阶段，也没有知识写入。

策略候选上限现为 32 张，优先保留用户锚点，再按目标词义、锚点词义、共同来源、共同领域及既有关系端点排序；外发目录进一步限制摘要、来源和关系摘要。策略阶段使用独立的 24,000 Unicode 字符输入上限，不再填满执行阶段的 60,000 字符额度。直接说明为空时回退到已经生成的侧重目标。策略输出上限调整为 16,384 tokens，并要求理由与排除项简洁。对尚未产生知识写入的第一步输出截断，用户显式继续后使用新的恢复轮次重新请求策略；失败响应和原请求账本保留，重复无进展仍由恢复守卫熔断。

用上述真实任务和当前 628 张正式卡只读重建请求，输入为 23,757 字符、27 张候选，两张用户锚点均保留，随后候选优先出现房地产软着陆、土地财政城市建设、要素市场改革和资源型城市转型等相关对象。没有发送模型请求。策略、恢复及相关建构契约回归 71/71、前端 235/235、TypeScript 与生产构建通过。后端全量 769 项中 744 通过、13 失败、12 跳过；失败仍集中在已移除的 `commitBuffer` 旧测试、未跟随 `credentials.describe` 的凭据夹具、设置并发及既有生命周期断言，不能写成后端全量通过。

当前 3093 已加载修正后的恢复判断：该真实失败任务由“没有安全继续路径”变为“重新请求策略与选卡”。本轮没有点击继续、没有增加供应商请求、没有执行建构或修改正式知识。Browser plugin 不可用；使用本机 Edge / Playwright 在 1440×960 只读确认正式页面可加载且当前没有运行中的任务。

## 2026-09-19：纯图片封面识别与原书复核归档

EPUB 预处理现在只在同时满足“封面标识或封面命名、正文为空、引用的包内图片真实存在”时，把纯图片页认定为封面并保留提示，不再把它计作正文提取缺口。普通空白章节、图片型正文页、缺失资源、路径异常和无法解析的 spine 项仍标记为 `incomplete`，继续阻止自动移除 Inbox 原书。

对于已经完成全部阅读单元、但历史提取记录仍为 `incomplete` 的任务，公开任务状态提供 `book-archive-review-v1` 复核入口。界面完整展示冻结警告，用户明确确认后，Gateway 重新核对任务版本、原件与提取版本、全部单元完成状态和 Archive 原件哈希，再以可恢复事务仅移除对应 Inbox 条目。历史提取警告和任务结束状态保留；版本变化、未完成单元、运行中任务及同名新文件仍拒绝归档。关闭任务会保留关闭前的具体停点说明，不再只留下通用结束文案。

真实 EPUB《转型中的地方政府：官员激励与治理》只读重提取仍得到 66 个正文单元和 29 张图片，纯图片 `titlepage` 被识别为封面，`incomplete=false`，原件指纹保持 `2029df3664318864b4c04fed1d8c22d1d10fbe7962cabe96a60c92153c69166d`。该历史任务经界面显式复核后生成归档收据，Inbox 条目已移除，Archive 原件哈希一致，66 个单元和知识卡未重编译。

验证：EPUB 专项 7/7，图书编译、存储与入口专项 100/100，前端相关 60/60，TypeScript 与生产构建通过。后端全量 769 项中 744 通过、13 个既有失败、12 跳过；失败仍集中在已删除旧接口、凭据测试夹具和既有工作生命周期断言，不属于本次归档链路。正式 3093 已重载，健康接口返回 `uno_book_archive_review=book-archive-review-v1`。Edge / Playwright 在 1440×1000 和 390×844 验证任务页、警告、按钮、归档后状态、无横向溢出、无框架错误及相关控制台错误；Browser plugin not available。

## 2026-09-19：建构策略响应尾部闭合恢复

新建构任务 `998f32ab-45c2-4683-96f7-3dd78f67690e` 的第一次策略请求已返回完整策略、6 张选卡和单个工作包，但最外层 JSON 少了最后一个 `}`，因此停在 `strategy_pending / INVALID_GENERATION_RESPONSE`。原响应已完整保留，任务没有进入作者阶段，也没有知识写入。该次请求上下文为 59,540 字符，没有触发旧 128 KB 输入限制。

建构专用解析现在允许一种零歧义恢复：响应在完整 JSON 值之后只缺少尾部容器闭合符时，按现有 `{`／`[` 栈唯一确定并追加 `}`／`]`，随后重新执行原 JSON 与阶段契约校验。未闭合字符串、括号错配、缺逗号及其他语法错误仍失败关闭。恢复元数据写入对应调用记录，原始响应不覆盖。策略阶段的恢复计划复用已完成调用，不再次发送策略请求；通过契约后才进入作者工作包。

对该真实响应只读重放时仅追加一个 `}`，完整策略校验通过，冻结选择为 6 张卡、1 个工作包。建构、恢复计划和相关宿主回归 31/31，TypeScript 与生产构建通过；仍保留既有主包体积提示。本轮没有调用模型、恢复真实任务、写入知识或重启正式服务。

## 2026-09-19：策略先行的独立建构服务

新建构入口已切换为 `strategy-driven-v2` / `uno-construction-v1`。普通建构先用一次模型请求制定策略与选卡方案；宿主验证必选锚点、候选身份、操作权限、24 张总上限和每包 6 张上限后冻结计划。全库关系编织则由宿主根据知识图直接冻结一个焦点卡和最多五个只读关系端点，不再为选卡单独调用模型，也不要求用户预先指定卡片。随后由独立建构服务按包发出无历史、无工具的作者请求和审核请求；具体问题最多进行一次局部修复和复核，通过后仍由 `HarnessGateway` 暂存、发布并记录收据。新流程不再通过原生 Agent 会话驱动。历史 `direction-driven-v1` 任务保持原协议，只恢复、不改签。

“发现联系”或允许新建关系的“综合整理”现启用 `incremental-relation-weaving-v1`。宿主每轮只安排一个最多六张卡的关系工作包，且只允许焦点卡产生关系写入，其他卡仅作只读端点，避免为一条连接生成成对或批量反向修改。结算后从当前 Markdown 卡片与正式关系重新计算弱连通分量：先逐项检查完全孤立卡，再检查当前最大连通分量之外的多卡知识岛。当前版本已经完整检查但没有可信关系的对象会登记为“保留独立”，不会在图未变化时重复请求，也不会为了降低孤立数强制连边。关闭连续处理时每轮暂停；继续任务从最新图重新规划。准备接口与健康接口分别声明 `uno_relation_weaving=1` 和 `relation_weaving_contract=incremental-relation-weaving-v1`。

建构证据包只交付被冻结的完整卡片、有界来源摘录和当前领域定义。涉及正文、类型、摘要、边界、合并或来源关系的修改必须引用本次实际交付的精确来源片段；导航关系可依据卡片正文。改变关系时两端都必须在冻结选择内并显式声明 `basis`。关系编织的作者与审核请求不再重复嵌套策略、选卡、完整主图 ID 和整段来源；审核只携带实际写入焦点的旧状态、草稿状态及被引用原句附近的来源摘录。确定性预检问题局部延期，不再为了无效端点单独追加模型修复。仅调整关系的草稿采用关系差分校验，保留旧卡正文和旧元数据，不再因历史卡缺少新版 `summary` 或 `boundary` 前置字段而拒绝合法关系更新。领域新建和成员治理仍走独立领域事务。

本次故障任务实际记录为策略 23,797 字符、作者 46,816 字符、局部修复 37,609 字符三次已发送请求，随后审核在本地以 63,375/60,000 字符被阻止，未发送。超限主要来自主图 353 个 ID、策略与选卡在工作包内重复嵌套，以及审核同时携带修改前后完整卡片和大段来源。上述重复项已经从关系编织路径移除；正常成功轮次现在固定为作者一次、独立审核一次，只有审核提出可修复的具体语义问题且仍有额度时才可能追加局部修复与复核。若本地上下文检查仍在外发前阻止请求，恢复计划允许保留作者结果与草稿、重新生成一次精简上下文；若状态没有进展，现有恢复保护会停止重复空转。

验证：建构、关系编织、控制与恢复计划专项 56/56，建构面板 23/23，TypeScript 与生产构建通过；Vite 保留既有主包体积警告。Edge / Playwright 在 1440×960 和 390×844 验证默认“全库自由建构 · 无需选卡”、未选卡时开始按钮可用、无横向溢出及相关控制台错误；Browser plugin not available。界面验收没有点击开始。截图保存在 `%TEMP%/uno-free-construction-ui/`。

本轮没有调用真实模型、执行真实建构、写入正式知识实例或重启 3093 服务。源码、合成测试和构建结果不证明正式服务已加载，也不证明真实模型能够稳定提出高质量建构策略；正式加载和真实样本效果需单独授权验收。

## 2026-09-19：未组织池正式卡的专用单卡重编译

“重编译正文并整理领域”已从通用建构 Agent 分离并升级为 `single-card-source-rewrite-v2`。首次请求只包含当前卡与它已绑定的局部来源，要求从空白重写 `title/type/summary/boundary/body`；ID、来源和关系由宿主冻结。独立审核使用新的无历史、无工具请求；只有存在具体问题时才增加一次局部修复与一次复核。局部修复和复核都只携带候选卡、问题和短证据，不再携带原文、旧卡、领域目录、其他卡片、工具定义或会话历史。宿主要求每个被点名字段确实改变，同时拒绝改写未被点名的字段，防止原样返回或借局部修复重写整卡。正文通过后，再用一个独立请求从最多 8 个既有领域定义中核对归属；直接通过通常共 3 次请求，需要局部修复时通常共 5 次，累计额度仍为 6。

确定性预检和模型审核通过后仍由 `HarnessGateway` 暂存并发布；版本变化、来源不足、响应不合约或复核未通过时，原正式卡保持不变，问题记录在独立任务中。领域核对只读取当前卡和既有领域边界，不再要求正文模型返回领域；确认后由 `HarnessGateway.applyDomainGovernance` 原子挂靠并退出未组织池。若没有可靠的既有领域，任务保持 partial，卡片继续留池；单张卡不能据此新建领域。界面另提供“只整理领域”，对正文已经合格的卡仅发出一次领域核对请求，不重写正文。正文已发布后若领域请求中断，收据和修订保持，继续任务只恢复领域步骤。

用户此前对 `methodological-individualism-government-behavior` 连续执行的两次真实单卡重编译均属于历史 v1：第一次使用重写、审核 2 次请求并发布；第二次使用重写、审核、局部修复、复核 4 次请求并发布当前修订 `ac21f397f5717822dd84d496c2b5940d377968a77c549a1fb374e93477df407b`。两次任务都冻结了 `domains: []`，因此正文发布后仍留在未组织池；这正是 v2 增加独立领域步骤和“只整理领域”入口所修复的问题。第二次运行暴露出的复核重复携带 7,408 字来源以及审核过度挑错也已修正。当前正式卡尚未自动改写领域，仍需用户从未组织池执行新入口。

单卡、领域治理、提示词记录和卡片管理专项后端 44/44、前端 234/234 及生产构建通过。合成回归覆盖领域单独整理、正文直通后挂靠、局部修复后挂靠、无领域匹配保持 partial、正文发布后领域中断与恢复；领域单独整理只发出 1 次无来源正文请求。当前代码下的局部复核请求仍为单条消息、`source_chars=0`，原样返回和越界字段修改均停留为 partial 且不发布。正式 3093 已重载，工作区身份一致，健康接口返回 `uno_single_card_recompile=single-card-source-rewrite-v2`；桌面 1440×1000 与移动 390×844 实际渲染均显示“只整理领域”和“重编译正文并整理领域”，无相关控制台错误或横向溢出。本轮没有再次调用真实模型，因此不把合成结果表述为新的模型质量验证。

## 2026-09-19：编译隔离与未组织池单项修复

`compile-isolation-v1` 已连接主线编译、未组织池、独立修复、Gateway 发布、领域待组织状态及父子任务收据对账。通过卡先保存；超过有界恢复次数的局部问题留池继续，连续三个无可用候选单元熔断。待修复候选与已发布无领域卡明确区分，隔离不代表完成。单卡修复保留最新失败候选；待补关系依据修复后的两端复核；响应恢复只处理选定内容；截断前完整候选保留并携带精简清单续写。详见[编译失败恢复](UNO-编译失败恢复.md)。

后端相关回归 121/121 通过；最后的恢复、计数与早期上下文停点调整另验 74/74、隔离专项 15/15。前端 234/234，TypeScript 与生产构建通过。全量后端运行记录为 752 项：727 通过、13 失败、12 跳过；13 个失败名称与上一轮已在修改前副本复现的基线一致，涉及旧导入、凭据模拟及工作生命周期。本次不把全量标为通过。生产构建保留已有大包提示。

专项使用临时合成实例、真实 Gateway 和注入的模型响应，不使用付费模型。覆盖主线隔离后继续、非法候选身份、一次格式恢复、空响应一次重试、连续失败熔断、预算和停止不被隔离、版本／知识库冲突、修复幂等、失败候选保留、截断续写、待补关系重检、缺失收据拒绝对账与最终原件归档。

Edge / Playwright 在 1440×1000 和 390×844 验证“编译进度→未组织池→局部修复→修复任务”、原始响应与 JSON 校正编辑；页面非空、无框架覆盖层、无相关控制台错误或横向溢出。Browser plugin not available。页面中的问题候选与提交返回仅在浏览器拦截模拟，所有实际写请求被阻止；API 与 Gateway 另在临时实例验证。真实第 26 单元任务仍显示“审核已保留候选并继续”，没有点击继续。

正式本地 3093 已重载，工作区标识核对一致，`capabilities.uno_compile_isolation=compile-isolation-v1`；真实未组织池返回分类字段。重载前后 3,160 份正式卡片及任务文件哈希一致，真实编译仍 paused、调用记录仍为 61。未启动真实模型编译，不据程序或模拟页面测试宣称真实模型质量已通过。

程序备份：`%TEMP%/uno-isolation-20260919-004711/`。日志：`%TEMP%/uno-isolation-{targeted,tests,recovery-final,web,full,build}.log`。页面证据：`%TEMP%/uno-isolation-{desktop,mobile}.png` 与 `uno-isolation-browser-result.json`。回撤只恢复对应程序差异并重建、重载；不覆盖知识、任务、原文和账本，隔离／修复任务先停止，不交给旧控制器继续。

## 2026-09-19：制卡覆盖说明缺失恢复

任务 `a40ee7bf-4d57-46e2-ac7d-ba5251024d7a` 在第 26 个单元“4.5 省以下政府间的财政分成”停于生成响应校验。原返回为完整可解析 JSON，含 8 张候选，但没有顶层 `note`；没有输出截断或候选超过上限。此前统一报错将其归入不可重试的知识审核失败。

非空且满足基本结构的候选现在可确定性标记覆盖说明未提供，保留原返回并进入正常审核，不重发原文或调用格式恢复模型。空候选缺理由、候选身份或结构错误、历史 v2 以及语义审核失败仍保持限制。旧停点的恢复入口由只读计划提供，任务文件不因读取而修改。详细规则见[编译失败恢复](UNO-编译失败恢复.md)。

相关程序回归 87/87、前端 233/233、TypeScript 与生产构建通过。全量后端运行 722 项：697 通过、13 失败、12 跳过；13 个失败点在替换回本轮修改前程序文件的隔离副本中全部复现，涉及旧导入、凭据模拟及工作生命周期，不计为本次回归通过。构建仍报告既有大包提示。

正式本地 3093 服务已重载，健康接口返回 `uno_generation_note_recovery=1`，真实任务 GET 返回恢复计划和“审核已保留候选并继续”。Edge / Playwright 在 1440×1000 下打开真实任务并核验按钮启用、原决策面板消失、页面非空、身份正确、无错误覆盖层或控制台错误；Browser plugin not available。未点击继续、未调用真实模型，未验证后续真实模型审核效果。重载前后核对 3,160 份卡片及任务文件哈希一致，实际任务仍暂停。

程序备份位于 `%TEMP%/uno-generation-note-20260919-003122/`；测试日志为 `%TEMP%/uno-note-{related,full-tests,baseline-tests,web-tests,build}.log`，页面证据为 `%TEMP%/uno-note-recovery-desktop.png` 与 `%TEMP%/uno-note-browser-result.json`。回撤仅恢复本轮对应程序差异并重载服务，不覆盖卡片、原件、任务与请求账本。

## 2026-09-18：领域待确认提示

编译任务进入领域确认时，面板顶部固定显示黄色提示、待确认数量和“请向下滚动确认”；“前往确认”直接定位并聚焦领域提案。确认或暂缓后离开审核状态，提示随之消失；只读历史和结束中的任务不显示可操作的确认入口。

前端专项 16 项、TypeScript 和生产构建通过。正式 3093 提供新版前端；Edge / Playwright 在 1440×1000 与 390×844 下验证页面身份、非空显示、无错误覆盖层、控制台无相关错误、滚动定位、键盘焦点、模拟确认后消失及结束状态隐藏。Browser plugin not available。待确认状态和提交返回仅在浏览器内模拟，真实服务仅做读取，没有领域写入、模型请求或后端重启。截图、校验结果及本轮两份程序文件备份位于 `%TEMP%/uno-domain-notice-20260918/`；回撤仅恢复对应前端差异并重建。

## 2026-09-18：编译同名卡核对

新版编译遇到未完整提供的同名旧卡时，局部交付两张完整卡，由模型判断复用、合并或另建，继续通过 Gateway 审核与写入。原文不重发，版本冲突仍拒绝，已完成核对随任务恢复。102 项相关测试通过；正式任务已越过原卡住单元，由 2/15 推进至 3/15。范围、证据与完成边界见[编译同名卡核对](2026-09-18-编译同名卡核对.md)。

## 原件与拆分材料存储

新提取的原件仍保存在 `03-Archive/books/<原件哈希>/`；Markdown 单元、目录和提取图片保存到 `05-Buffer/books/<原件哈希>/<提取哈希>/`。Gateway 按版本保全并使用独立的存储回执键，原书在整本处理完成前仍留在 Inbox。读取器、来源校验、建构发布、图片接口和 Web 来源链接支持新位置与原 Archive 路径；参考卡与同源复用比较原件版本、提取版本和单元编号，不因目录前缀变化而丢失同源识别。Buffer 单元不自动进入普通对话全量检索索引。

验证：图书编译、写入、恢复、来源阅读与建构发布相关测试 83 项通过，卡片阅读前端测试 7 项通过，TypeScript 与生产构建通过。在隔离样本中用真实 Gateway 和 Web 读取接口完成卡片→Buffer 原文→Archive 原书链接的浏览器验证，Buffer 图片加载正常。1365×900 与 390×844 下页面非空、无错误覆盖层、无相关控制台错误或横向溢出。使用 Edge / Playwright，Browser plugin not available。截图与临时夹具保存在 `%TEMP%/uno-buffer-reader-LM9PBg/`。

2026-09-18 经用户明确授权，已备份并迁移“中国政治经济”库的 9 份提取版本：77 个阅读单元及其目录、图片共 167 个文件从 Archive 移入 Buffer。《战略与路径：黄奇帆的十二堂经济课》19/19 个单元已处理；其两处无正文 EPUB 页面经打开图片核对均为封面，原件已通过 Gateway 移出 Inbox，Archive 原件哈希保持不变。历史提取警告和已结束状态保留，维护核对及归档结果登记在事务收据和任务归档记录中。未完成原件继续留在 Inbox。

本轮相关后端测试 81 项通过。正式 3093 服务已停止旧进程并重启（验证时 PID 7388）；随后实际请求正式接口，77 个旧来源链接返回正文、81 个旧图片链接的返回字节哈希一致。306 张知识卡文件哈希不变，无任务执行中，没有真实模型请求。备份、迁移计划、执行收据清单与正式接口核验记录位于 `%TEMP%/uno-storage-backup-nYQSJI/`。

后续正常编译先归档满足完整性要求的原件，再进入领域治理检查点；待确认领域提案不阻塞原件归档。存储迁移只在明确授权的维护操作中执行，不随读取自动搬运。回退须保留来源兼容读取，或先从备份恢复对应 Archive 文件并验证引用；不能只回退读取代码而把已迁移来源留在不可读位置，也不能覆盖后续卡片和任务变化。

## 2026-09-18：编译成卡资格与目录关系提示

编译方法、正反例和实际请求提示加入独立知识对象判断。纯目录、全书路线图、章节主题拼盘、阅读顺序及纯编务信息不成卡，不得改名为模型或框架；序言、附录等位置的实质知识仍可提取。完整阅读后只有导航内容允许返回零张卡，不将其当作遗漏。关系判断排除仅由书籍归属、章节顺序或大纲包含产生的 supplement、specialization，basis=navigation 也不能替代两端的实际知识联系。

规则覆盖生成、截断续写、遗漏补全、常规候选审核、原遗漏复核和局部修复。常规审核仍不携带原文，须依据候选正文指出具体问题，不能按标题、空领域或来源位置拒绝。局部修复保持同 ID 和原问题范围，不编造内容、返回 null 或删除候选；无法有据修复仍按既有两次上限暂停。本次没有新增候选淘汰流程，也没有清理已有卡片或关系。

验证：`node --test tests/uno-book-agent.test.mjs tests/uno-book-host.test.mjs tests/compile-review-governance.test.mjs` 通过 56/56，JavaScript 语法检查通过。新增合成测试证明零卡单元正常结算并保留可追溯原文、成卡资格意见不会被领域治理过滤、被审核指出的大纲候选不能靠改名发布且其他有效卡仍保存。请求构造测试覆盖各提示分支和无原文局部上下文边界；这些结果不证明真实模型识别质量。本次没有调用付费模型或写入正式知识实例。

提示文件在模块加载时读取，当前修改需要重载 UNO 后端后才能用于新请求；此前正式服务重启被自动审批拒绝，本轮未绕过该限制。修改前程序备份在 `%TEMP%/uno-compile-admission-20260918-164029/`；撤回只恢复本节对应提示与测试差异，不覆盖知识、任务、预算账本或其他开发修改。

## 2026-09-18：编译收尾与继续入口

真实任务 `b2e8bd8c-8cef-498b-aa2d-13f20ef84b94` 已处理 19/19 个可读单元，记录保存或丰富 170 张卡片；来源仍有提取警告，领域未组织池为 15 张。只读检查表明提取警告对应 EPUB 的两处封面图片；没有据此改写原提取收据或宣告整书完成。隔离副本恢复没有新增模型请求，随后再次返回 partial/done，确认存在无剩余阅读工作的空续跑；未复现独立的运行异常。

继续资格改由实际剩余工作判断。已结算且只有来源完整性警告或独立领域待办时，公开接口声明不能继续；resume/retry 在预算补充、原生会话查询及任务启动前拒绝。阅读延期、尚未处理单元、提取或归档失败仍保留恢复入口。完成摘要区分原件核对、归档和领域组织，历史修复错误不再作为当前失败展示；读取旧记录只投影当前说明，不改写已保存任务。

页面显示“本轮结束 · 有待办”，移除无效继续入口和旧响应恢复提示，保留关闭记录、成果与待办。工作状态投影使用 closed，不能把有待办解释为全书已完成，也不再显示为可继续的暂停。

验证：编译、来源事务、入口及 UNO 会话专项 83/83，前端定向 62/62，TypeScript 与生产构建通过。扩展旧工作生命周期测试另有一条建构自动接续断言失败；修改前源码副本也复现相同失败。Edge / Playwright 在 1440×1000、390×844 核对实际页面身份、非空内容、无运行错误、无横向溢出、结束提示、无效按钮消失和阅读记录展开收起。Browser plugin not available。没有付费模型调用；557 个卡片和归档文件与检查前副本一致，真实任务 version=1316、实际请求=151 未变。

当前生效边界：前端 dist 已更新并由 3093 提供；后端停止并重启被自动审批拒绝，仅返回 blocked by policy，未加载 `uno_compile_completion=1`。后端接口保护、旧摘要投影和全局工作状态仍需按本项目 stop-nexogenesis.cmd、start-nexogenesis.cmd 顺序重载后核对健康接口。本次未自行重编译、改写原件或处理领域待办。

验证日志、截图、修改前程序和真实任务隔离副本保存在 `%TEMP%/uno-completion-20260918/`。撤回仅恢复该目录 before 中本轮程序差异并重建前端，不覆盖知识文件、原始材料、任务和预算账本。
## 领域说明与领域关系基础

领域记录使用共享的定义校验，阅读接口提供核心问题、纳入与排除范围、代表卡、上级领域和直接成员数量；人工正文保留。`HarnessGateway.writeDomain` 支持部分字段更新，省略字段不覆盖，经过版本、幂等、历史与事务保护。领域关系限定 adjacent/contrast/bridge，保存用途和适用边界，basis 为 navigation；父子层级独立。图谱使用稳定领域边 ID，阅读界面支持出向和入向入口，入向显示不创建反向关系。

相关后端回归 29 项通过，前端专项 9 项通过，TypeScript 与生产构建通过。最后的领域专项回归 14 项通过。隔离浏览器验证了 A→B→C、返回、代表卡跳转以及窄窗口换行；600 成员测试不把成员正文加入领域详情。正式 3093 的制造业领域接口已返回 3 个核心问题、3 项纳入范围、2 项排除范围、3 张代表卡和 23 张直接成员；这只是实际接口证据，不代表检索质量提升。最终停止并重启正式服务的命令被自动审批拒绝，因此最后全部源码是否已加载仍未确认。

本次未调用模型、未改正式领域或成员卡、未自动生成领域关系。自动发现和建立关系留给后续建构，检索路由与跨域扩展不在当前范围。参数与例子见[领域契约](design/knowledge-guidance/domains.md)。

## 2026-09-18：继续额度与常规审核范围治理

新建编译把 `120` 解释为一次明确继续后应有的可用请求额度，而不是从任务创建起永不恢复的总池。点击继续保留历史请求账本和实际消耗，只在当前剩余额度不足时把总上限提高到“已用量 + 本轮额度”；未发生新请求时重复点击不会继续叠加。任务公开 `resume_budget_calls`，页面显示“继续后可用请求额度”。

常规候选审核使用 `routine-review-governance-v2` 确定性治理。该环节只审核卡片内容与关系，不判断领域语义适配，也不要求模型验证由宿主绑定的当前单元来源；七种正式关系类型全部写入审核提示。首次审核与局部复核都会对账真实 Markdown 标题树，段内句子或项目符号不能被误报为额外小节；继续旧检查点前，宿主剔除越权领域意见、过期来源意见、把合法关系误判为非法枚举的意见，以及与真实标题树矛盾的层级意见。只有问题集合实际发生变化的卡片才重新开放对应局部修复次数，未变的语义问题不会借继续无限重试。

未组织池中的单卡与关系待办使用 `isolated-repair-diagnosis-v1`。新任务先让模型把历史失败记录与当前卡片重新对照；关系待办同时完整提供另一端卡片。诊断只保留仍成立且可以执行的问题，并明确字段、段落或关系操作；它会替换旧问题，而非在旧问题后继续叠加。用户补充是可选的保留边界或证据提示，不再伪装成新的 `card` 错误。“关系成立”“可以保留”等肯定结论由提示词与宿主治理共同阻止进入问题队列。随后才执行局部修复和独立复核；明确删除旧关系仍由宿主确定性核验，不增加模型调用。

专项回归覆盖：继续补足额度且不清空账本、重复点击不叠加、常规审核范围过滤、合法 `example` 误判剔除、同一旧记录中保留真实关系问题并只执行一次关系修复。前端定向回归 54/54 与生产构建通过；预算旧套件另有凭据测试夹具未跟随当前 `credentials.describe` 接口的既有失败，不能写成全套通过。

正式 3093 已重启并返回 `uno_review_governance=routine-review-governance-v1`。真实任务 `b2e8bd8c-8cef-498b-aa2d-13f20ef84b94` 从第十一章的 `UNIT_CARD_REPAIR_EXHAUSTED` 恢复：确定性对账剔除宿主来源、领域语义及合法 `example` 误判四条旧意见，只对剩余关系问题继续；处理进度由 15/19 变为 16/19，两个阻塞候选均保存，来源锚点落到实际第十一章，供应商账本由 138 次增至 142 次。随后领域治理提出一个仅有单一材料单元证据的新领域，按人工模式暂缓后，任务已进入第十二章生成。这证明原无响应死锁已跨越；不代表剩余全书或新领域语义已经验收完成。

## 2026-09-18：编译响应恢复监督层

源码增加 `compile-response-recovery-v1`。单元检查与单卡／关系复核响应先走确定性归一化：常规无原文检查已经返回完整 `checked_ids` 和 `issues`、仅缺 `unit_issues` 时，补入必需空数组并重过原校验，不增加模型请求。其他可安全处理的 JSON／外层结构歧义最多调用一次局部恢复，输入不含原书、候选卡正文、参考卡或历史；模型不能改写原问题，结果仍经过 ID、关系端点、审核范围和原 `validateReview` 校验。

每个失败响应使用阶段、请求键和原返回生成指纹；同一指纹最多一次模型恢复，每个任务最多三次。恢复失败后继续任务不会重复支付同一响应修复。任务公开 `last_recovery` 和 `last_failure`，提示词检查器单列“编译 · 响应恢复”。响应 Hook 不处理审核职责越权；常规审核意见由上方的审核范围治理另行对账。上下文、版本冲突、持续语义失败、用户停止和输出截断分别沿用自身处理。

专项单元编译回归为 33/33；关联后端回归 120/120、前端全量 224/224 通过，TypeScript 与生产构建通过。覆盖零调用恢复、一次局部恢复、失败后熔断，以及既有输出截断、关系修复、单卡修复、Gateway 收据恢复。全量旧后端仍有 13 项既有失败，集中在已经删除的 Buffer Gateway 接口、旧凭据模拟和旧生命周期断言，不能写成全套通过。

正式 3093 已从旧 PID 14824 重载为 PID 2000，workspace ID 核对一致，健康接口返回 `uno_compile_recovery=compile-response-recovery-v1`。真实任务仍暂停在 82 次调用，未自动继续、未新增模型调用、未改知识卡或原书。项目增加 `stop-nexogenesis.cmd`／`.ps1`：先核对健康身份、workspace ID、端口和进程命令，再只停止本工作区服务；重复双击启动器不会替换已经运行的旧进程。

## 2026-09-18：领域治理核心生命周期

源码已补齐空知识库的领域生长闭环。`domains: []` 卡片写入可重建未组织池；编译从已有 `book_outcomes` 恢复检查点计数，在 10 个有效单元、20 张新未组织卡或有足够材料的书末边界触发一次治理。每批最多 20 张，只发送最多 5 张完整卡正文，其余为摘要；不发送原书、不读取 Buffer、不进入单卡修复循环。

已有领域挂靠由 `HarnessGateway.applyDomainGovernance` 批量提交。新领域形成运行态提案，Web 显示核心问题、纳入／排除边界和首批成员；默认由用户逐项批准或暂缓。创建编译任务时也可选择“允许编译自行决定领域”，把自动批准方式冻结到本次任务。两种方式都使用同一个幂等、版本校验事务同时写入正式领域 Markdown、成员卡历史和全部 `domains`，失败不产生半个领域。知识处理进度与领域组织进度分别显示。

程序验证：领域治理、单元编译、图书存储、分类契约与知识契约相关回归 86/86 通过；前端与 API 相关回归 53/53 通过，TypeScript 与生产构建通过。正式 3093 已重载，健康接口返回 `uno_domain_auto_approval=1`，准备接口返回 `domain_approval_modes=[manual, automatic]`。浏览器核对确认现有旧任务仍显示“新领域需逐项确认”，不会因部署新功能自动获得授权；当前真实编译任务保持暂停，未增加模型请求、未改写知识卡。新建任务的自动批准模型质量仍需在用户实际勾选后观察，程序回归不能替代真实领域边界验收。

当前未实现边界：独立存量全库治理按钮、知识库建设方向编辑器，以及领域 split/merge/reparent/retire。旧建构的 `domain_proposals` 直接写入分支已移除；恢复时只保留延期审计记录，不能冒充领域治理能力。完整现行契约见 [领域治理方案](UNO-领域治理方案.md)。

## 2026-09-18：卡片与关系修复分流

源码审核策略升级为 `review-publish-repair-v2`，并声明 `uno_relation_repair=1`。审核问题使用 `kind=card|relation`：卡片问题仍只携带本卡和具体要求，不准新增关系目标；关系问题携带本卡和明确、完整核对过的当前端点，只准修改 `relations`。两类分别计数与复核，关系端点进入请求键，端点变化会使旧响应失效。

针对真实任务出现的“原卡关系为空、审核要求新增关系、单卡守卫又禁止新增”的死锁，恢复逻辑不会接纳旧的单卡修复响应，而是按当前两端重新执行关系修复。回归覆盖空关系新增、过期关系意见、旧检查点迁移、单卡权限不放宽和 Gateway 保存；正式服务是否加载仍须另行核验。

正式 3093 当前仍返回 `review-publish-repair-v1`，没有 `uno_relation_repair`；原任务保持暂停，没有恢复或新增模型调用。源码准备和前端构建已完成，但自动执行策略拒绝停止并重启已确认属于本工作区的进程，只返回 `blocked by policy`，未提供更具体原因。因此本节的 v2 行为尚未由正式进程加载证明。

## 2026-09-18：分类、领域和提示词统一

现行源码分类契约已升级为 `ordered-single-type-and-domains-v2`。新卡按 `conflict/entity/case/concept/method/mechanism/model/claim/phenomenon/undetermined` 顺序检查每类的全部最低成立条件，取第一个合格类型。`phenomenon` 需有可观察对象与依据，不作无条件兜底；九种实质类型均不成立但内容仍完整、可独立阅读且可复用时使用 `undetermined`。此调整不自动改写存量卡片。

运行核验：正式 3093 健康接口已返回 `card_classification=ordered-single-type-and-domains-v2`；准备接口按契约顺序返回九种实质类型与 `undetermined`；图谱总览返回同一 `classification_scope`。新建编译和建构任务都冻结 v2 分类字段，建构作者、独立审核、工具 Schema、类型指南与阶段证据包读取同一冻结版本；核验时无执行中知识任务，现有的1个暂停 v3 编译任务未保存分类字段，恢复时继续使用它开始时的 `single-type-and-domains-v1`，不中途混用新口径。本调整没有发送付费模型请求，没有恢复任务，也没有迁移或改写存量卡片。

源码中的新建编译标识为 `unit-cards-v3` / `uno-unit-compile-v3`，审核为 `review-publish-repair-v2`。新卡仅使用单一 `type` 与零到三个既有 `domains`，不写 `tags/topics`；卡片自身领域数组是成员归属唯一事实源。生成与审核提示都注入类型、领域、正文骨架的解释和正反例；卡片返工只携带本卡，关系返工只增加明确端点。参考卡组包为 3 张完整卡加 5 张导航摘要；普通对话思考深度不再传入新建编译或建构。自 2026-09-21 起，新任务冻结 `provider-aware-v1` 资源快照：百万级上下文模型允许 90,000/90,000 字符的独立原文与其余上下文，256K 模型为 72,000/72,000；能力足够时制卡输出最多 98,304 tokens、修复最多 49,152、审核最多 16,384，并始终受精确模型输出上限约束。截断时只保留完整候选，继续时请求剩余卡。旧任务按原快照或原常量恢复。v2 仅用于恢复已保存任务。以下 2026-09-17 记录按当时行为保留，不能覆盖本段现行契约。

上一次加载核验：正式 3093 进程已重启，健康接口返回当时的 `unit-cards-v3` / `uno-unit-compile-v3` / `single-type-and-domains-v1`；准备接口不再返回 `tags`，固定返回当时的九种主类型与 `domains` 目录。运行状态为 0 个执行中任务、1 个暂停任务，重启没有自动恢复编译。没有发送付费模型请求，也没有迁移或改写存量卡片。

验证边界：分类、v3 编译入口、建构分类冻结与前后端任务交接定向 42/42 通过；建构、证据包、发布、检索和图谱扩大回归 83 项中 81 项通过，2 项在加载阶段因旧测试仍调用已移除的 `HarnessGateway.commitBuffer` 失败，与本次分类路径无关。前端 36 个测试文件、233 项通过，TypeScript 与生产构建通过；Vite 仍报告 630.85 KB 主包的既有体积警告。正式 3093 已重载并通过健康、准备与图谱接口核验；未运行付费模型语义实验。完整后端仍有旧 Buffer Gateway、凭据模拟和设置生命周期测试债，不能写成全套回归通过。

## 2026-09-17：整章遗漏导致继续立即暂停

真实任务 `c5d27d14-18da-4887-a17e-7424fa37e8f6` 第 6 个阅读单元已有 11 张卡保存、单卡问题清空，但首次审核的 `unit_issues` 仍保存交易成本理论遗漏。旧执行器只有单卡修复分支，末尾永远因 `unit_issues` 抛错；恢复不清理或处理该列表，形成无模型调用的重复暂停。最后一次单卡核对已经通过，并非模型没有响应。

- 提示词要求能修复既有候选的遗漏指定卡片 ID；整单元遗漏有一次定点补全机会，最多 3 张新候选。输入只有原问题及其中证据、来源标识和已有卡的 ID/标题，不携带原文、旧卡正文或历史。只能新增独立遗漏对象，不能覆盖已保存卡。
- 新候选使用原文单独审核，限定为原遗漏和新卡，不重新评判已保存卡；随后仍按通过即保存、问题卡局部修改处理。单卡返工继续只带本卡与问题。没有新的不完整状态被标为完成。
- 补全响应、候选和检查进度持久化。恢复不重置次数、不重复补全；证据不足或一次补全后仍未解决时明确 `UNIT_COVERAGE_BLOCKED`，保留缺口与原件，不伪装整书完成。
- 37 项回归通过，含跨单元推进、补全后断线恢复、未解决时不重复请求、禁止重写通过卡、提示词输入输出记录。测试使用脚本化模型；同步补齐提示词测试里凭据服务的 describe 模拟接口。
- 真实任务只读副本恢复到下一条请求：supplement、1 条消息、原文 0 字符、其余 3,138 字符，包含 1 条遗漏与 11 张已有卡名称；11 张已保存卡哈希不变，没有模型请求。此验证不证明 Kimi 已补全该遗漏；正式任务保持暂停。

## 2026-09-17：单卡返回解析与重复请求缓存修复

真实任务 c5d27d14-18da-4887-a17e-7424fa37e8f6 于 20:35:19 暂停，原因是 Kimi 返回了完整的单卡 JSON 对象，而宿主只读取 value.card。宿主还在验证前保存了缺少 card 的 repair_response，继续执行时再次拒绝。该单元 8 张通过卡已保存，全库 13 张卡保留；此次停止发生在提示词返回记录功能上线之前。

- 单卡返回支持明确的 {card:对象} 或直接卡片对象，统一验证 ID 与基本形状后才保存待接收状态；错误 ID、含糊结构继续拒绝，内容/来源/关系检查仍由原 Gateway 和核对步骤执行。
- 对已有缺少 card 的检查点，只有原卡内容、问题、修复尝试及已保存响应的关联一致才接回原始响应，不增加修复次数或重新调用模型。
- 请求缓存键排除原生消息随机 ID；保留模型、提示词、卡片和问题等实际内容。旧修复响应可按已保存尝试直接消费，避免恢复时重付费。
- 24 项专项测试通过，覆盖直接对象/包装对象、错误身份、损坏检查点、变更后拒绝、修复次数上限、缓存及先保存后修复。同期更新两项未跟随上一轮提示词契约更新的旧断言。
- 真实任务隔离副本复用了修复响应，停在单卡核对调用之前：source_chars=0，other_chars=1995，仅 supplied_card 与 issues；没有新增模型调用，正式任务与卡片未改写。这证明恢复路径通过，不代表后续语义核对或整书编译完成。


## 2026-09-17：审查后先落卡，问题卡独立修复

正式 3093 已重载并核验为 `review-publish-repair-v1`，健康与准备接口一致，Web 显示“审查后先保存通过的卡，再单独修改问题卡”。专项后端 52/52、Web 入口与 API 51/51，TypeScript 与前端构建通过。真实失败任务副本在模型调用前保存了 11 张通过候选（10 张新增、1 张修订），既有最后响应已接回，待核对请求仅 4,528 字符、原文 0 字符。正式任务仍暂停、5 张正式卡未变，本轮新增模型请求 0 次；这不是新模型端到端成功声明。

当时策略为 `review-publish-repair-v1`；现已由上方记录的 v2 分流替代。该段仅保留当时正式服务和测试证据，不能作为当前接口说明。

1. 建卡：一次提供完整单元、有限参考卡、与 Gateway 相同的主类型正文规范及经过该规范验证的完整示例。模型只输出 JSON，不附修复说明。
2. 审查：程序一次收集逐卡规格问题，模型检查内容和关系。具体问题须写明改法及必要的短证据，避免修复阶段再读原文。可选扩展不作为阻塞理由。
3. 落卡：通过的卡立即经 Gateway 保存。指向尚未通过新卡的关系暂存在任务中，目标通过后补上；不保存幽灵链接。每笔提交保存幂等收据。
4. 修复：只提交问题卡及具体 issues，source_chars=0，无其他卡、整章原文、旧卡参考或历史。仅核对该卡与原问题，每卡最多两次模型修复；原响应重读不算新请求。
5. 恢复：已收到的响应先接收，已提交的收据先恢复，之后才判断是否需要模型请求。历史请求账本保留；明确继续时补足一轮可用额度。失败不撤销通过卡，不冒称全章完成，原书保留。

验证包含保存发生在修复之前、失败保留成果、延期关系补写、旧响应接回、写后中断收据恢复，以及实际请求中没有原文或其他卡。程序回归与历史响应回放不是新模型质量验收；不得据此宣称新书真实编译已经成功。

以下保留先前实施记录；与上述现行规则冲突的流程描述已被替代。

> 2026-09-18 更新：新建编译现行契约为 [单元编译实施](UNO-单元编译实施.md) 的 `unit-cards-v3`。v2 只恢复已保存任务；v1 阅读组与更早记录只作历史。源码声明不能证明正式服务已经加载本版。

# NEXOGENESIS-UNO 当前实现与验证

## 当前标识与源码事实（unit-cards-v3）

　　源码与准备接口的现行编译标识如下。健康接口由 `COMPILE_HEALTH` 与准备接口共用同一组字符串，避免文档再写 `book-reading-v1`。

| 标识 | 现行值 |
|---|---|
| `compile_profile` | `unit-cards-v3` |
| `compile_version` / `workflow` | `uno-unit-compile-v3` |
| `compile_review_policy` | `review-publish-repair-v2` |
| 健康数值 | `uno_compile_entry=4`、`uno_book_compile=3`、`uno_unit_compile=3`、`uno_unit_card_contract=2`、`uno_relation_repair=1`、`uno_legacy_compile_retired=1`、`card_classification=ordered-single-type-and-domains-v2` |

　　宿主一次提交完整 Markdown 单元（原文 ≤60,000 字符，其余上下文 ≤60,000 字符），直接调用模型服务生成 JSON 卡片，经 Gateway 预检和候选卡审核后，分别进入卡片修复或关系修复，每类最多两次。新卡分类只有一个 `type` 与既有 `domains`；提示词与 Gateway 使用同一冻结领域目录。无 `book_*` 工具循环、无阅读组、无历史正文叠加。已保存 v2 任务按旧契约恢复，v1 与更早任务只读。新建构使用 `strategy-driven-v2` / `uno-construction-v1`，历史 `direction-driven-v1` 任务按原协议恢复。

　　程序证据必须以本轮回归结果更新；此前 2 单元／4 请求及前端数字属于 v2 时点。没有新的付费模型验收，不能宣称 v3 语义质量通过。

　　正式进程是否加载 v3 必须查对应进程的健康、准备接口和真实行为；不能沿用历史加载声明，也不能由源码字符串推断。

## 2026-09-17：旧编译源码清理

　　删除旧摘要编译、章节筛选、Buffer 整理、聚合、消化、编译独立审核及对应写入控制器；普通原生对话不再注册这些工具。旧任务、旧提案与旧 Web 恢复入口均为只读或拒绝执行，历史原书、Buffer、卡片及记录不删除。当前建构从旧共享文件拆出为 `construction-host.js` / `construction-workflow.js`，本次不重设计建构。

　　单元制卡通过 Web API 和模型适配器完成本地模拟验证：2 个原文单元、2 张卡片、4 次请求、0 个工具错误，最终任务 completed 并归档。它证明调用链可运行，不是 Kimi 语义质量或长书稳定性的验证。本次没有付费模型请求。前端 219 项测试、TypeScript 与构建通过。

　　代码备份及应用清单保留在 `%TEMP%/uno-compile-retirement/` 与 `%TEMP%/uno-unit-compile`。仅应用相对基线的程序与构建差异，不覆盖知识库、材料或任务状态。清理版服务标记为 `uno_legacy_compile_retired=1`。

## 2026-09-17：图书编译 v1 重建（历史）

　　本节是已被 `unit-cards-v2` 取代的阅读组路径。当时隔离运行和正式服务加载已核验，并完成一轮有限真实 Kimi 实验；语义抽查仍发现问题，最后的方法调整未额外真实重测。不能当作现行入口。

### 当时实现的源码边界

- 新建入口为 `compile_profile=book-reading-v1`，任务为 `workflow=uno-book-compile-v1`。当时使用五个 `book_*` 工具；旧筛选、草稿发布或二审队列不恢复。
- 每个单元同时受 16,000 Unicode 码点、48,000 UTF-8 字节硬上限约束。同书相邻小单元按合计不超过 12,000 字符、最多 4 个一组；换组建立新上下文。
- 一次原子保存最多 3 张紧密相关卡片。模型来源只填 `ref`；程序为当前会话完整已读单元生成精确 `source_spans`。
- 未完成时原书保留在 Inbox；整本已处理且归档哈希核验通过后，Gateway 才移出 Inbox。

### 程序与隔离运行证据（v1）

| 验证 | 当时结果与边界 |
|---|---|
| 全后端 | 正式源码目录完整重跑共 715 项：703 通过、0 失败、12 跳过，用时 18.4 秒；日志 backend-applied.log |
| 图书专项 | 当时 45/45 通过，覆盖来源、写入、组间推进、完成归档及恢复等机械边界 |
| 来源查看 | 5/5 通过 |
| 前端 | 217/217 通过，TypeScript 检查与构建通过 |
| 真实浏览器 | 从新卡点击来源、打开真实归档正文，看到页章定位与原书链接；窄屏无异常 |
| 原生本地模拟 | 4 次请求处理 2 章节、保存 2 卡、0 工具错误，最终归档成功；使用模拟模型输出，不证明内容理解 |

### 正式部署与页面核验（v1，不可沿用为 v2）

　　正式 **3093 当时已加载 v1**。工作区身份为 `19e77ff31528bc9ddc61b3cafc81da6431cda6da506fd4f828d10c70743e37bb`；健康接口返回 `uno_compile_entry=4`、`uno_book_compile=1`，准备接口确认 `compile_profile=book-reading-v1`、版本 `uno-book-compile-v1`。

　　当时活动库「中国政治经济」为 **0 卡、0 原件、0 待处理**。正式浏览器显示当时的编译选材文案，Inbox 为空时开始按钮禁用，不显示历史 Buffer；该次部署验收没有调用模型。

　　这些证据只确认当时工作区加载了阅读组路径，不能证明当前源码的 `unit-cards-v2` 已在同一进程生效。

### 真实 Kimi 实验：11/12 次请求（v1）

　　该轮使用隔离复制的 **2,669 张金融卡片**底座，第三章材料为 **7,088 字符**。实际使用已授权独立预算 **11/12 次请求**，新增 **4 卡**，没有写入正式知识。剩余 1 次不使用。实验对象是五个 `book_*` 工具，不是宿主直提交单元。

| 计量 | 实际记录 |
|---|---:|
| input tokens | 34,209 |
| output tokens | 8,575 |
| cacheRead tokens | 104,448 |
| 最大请求正文 | 115,366 UTF-8 字节 |

　　三次工具拒绝中，两项是当时已修复的程序限制：要求作者重读自己刚保存成功的卡，以及不能结算提前处理的下一单元。第三项是将前言来源错误关联到正文章卡，触发 `RECEIPT_REQUIRED`，属于正确拒绝。

　　抽查 **3 卡**发现：模型估算的引用区间不完整、`example` 关系方向反向、可能的因果强化，以及遗漏“教育唤醒兴趣”的例外。原实验 4 卡保持原样。最后的方法调整未额外真实重测。现有结果不能支持“语义全面通过”或“整书验证完成”，也不能外推到 v2。

### 保全与撤回（v1 阅读组）

　　该轮隔离工作区为 `%TEMP%/uno-book-rebuild/work/`，修改前程序完整备份为同级 `before/`，基线哈希在 `baseline.json`。单元制卡另有 `%TEMP%/uno-unit-compile`。撤回前停止新任务并确认底层执行退出，按对应一轮差异恢复代码和前端构建；保留原书、提取版本、卡片、历史、任务、收据与实验记录。

## 历史时点记录

以下内容保留当时的故障、试验、授权和部署证据，均不再规定新图书编译流程。旧文中的“当前”“新编译”“仍待授权”“按原协议恢复”只描述当时状态；v1 阅读组实验已用 11/12 次请求、剩余 1 次不使用、旧编译不续跑等边界以上文为准。

<details>
<summary>展开 2026-09-16 及更早的实施记录</summary>

## 2026-09-16 晚间：筛选和内部会话交接故障修复

最新真实任务确认：新编排选择旧 Buffer 时仍使用原样字符串引文匹配，PDF 排版换行导致误拒绝；筛选通过后，本机宿主内部 HTTP 交接失败，作者尚未请求模型；本书目录行还曾被识别为正文标题。已经分别统一新任务的引文定位、使用原生进程内 RPC carrier 并保留部分代理兼容、修正目录标题识别。详见 [本次故障、重置与验证记录](UNO-编译筛选与会话交接故障修复.md)。

后端 699/699 回归通过，真实 PDF 重分章后正文内容保持；空闲时已重载正式 3093（PID 17712 → 13284），原生会话服务可用。用户确认后，隔离本书 32 份失败中间产物，并额外备份原书和旧提取文本，将原书复制回 Inbox。2672 张正式卡片及另一本 EPUB 保持不变；当前 Inbox 2 本，待处理旧单元 0。桌面与 390px 页面只读验收通过。本轮没有真实模型调用，完整编译质量及实际经济性仍待验证。下文较早的 PID、Inbox/待办数量及测试总数是历史时点记录，不能作为当前状态。

旧提取单元 ID 依赖章节序号，重新分章可能复用同名路径；历史结束任务的原文查看未版本钉住。旧提取文本已额外备份，不能据此宣称此版本问题已修复。

## 2026-09-16 编译按风险审核与恢复重构

本节是 2026-09-16 当时的编译契约与正式加载记录，已被 `unit-cards-v2` 取代。下文“新建编译冻结 review_policy=harness-first-v1”只解释该时点，不恢复为现行入口。完整现行说明见 [单元编译实施](UNO-单元编译实施.md)。建构及缺少新字段的旧编译不迁移。

新建编译冻结 review_policy=harness-first-v1。作者读取选定正文、形成草稿、登记当前版本的来源自检和材料去向；程序检查权限、版本、真实交付、引文定位与事务。普通单源新卡不默认全文二审。实际正文修订、合并、多来源综合、来源变更、来源型关系变更及明确疑点进入新的针对性审核上下文，最多返回修订一次。来源自检与独立审核分开记录；原句匹配不证明语义正确。

审核包仅含风险对象、实际差异和相关原文区间；修订包仅含待修对象及未完成材料。不成卡材料可凭作者已取得的全文及具体理由结算，不再强制二读。未发布草稿的错误来源可替换，既有正式来源保全。恢复先消费已保存的交接、收尾与成功收据；零预算时先本地结算合格成果，不再先开新会话，也不会自动放行风险对象。持久预算、128,000 UTF-8 字节完整请求限制和旧任务兼容保留。

- 后端 **681/681**（含已安装原生 DSH 专项，无跳过）、前端 **209/209** 及 TypeScript/Vite 构建通过。宿主组合测试使用合成模型输出，覆盖普通卡无需二审、source-only 不二读、假引文和过期证据拒绝、风险审核、一次修订、人工确认及恢复。合成场景的筛选和作者两次请求即可发布，只证明执行路径，不是实际书籍的请求量预测。
- 正式 **3093 已加载**：PID 25276 → 31708；health 返回 uno_compile_entry:3、uno_compile_review:1，原上下文治理能力仍为 1。重启前后均有 34 个空闲原生会话；**16,677 个**知识、原文、草稿、任务、收据与预算文件哈希一致，无新增文件。
- 实际前端 index-CshktCCq.js 已在 1440×1000、390×844 完成 **10/10** 只读场景检查。真实准备接口返回新审核契约、模型可用、Inbox 0、待处理材料 13。旧能力禁启、历史任务原协议提示、针对性审核和发布状态使用浏览器合成快照验证。无控制台错误、横向溢出或业务 POST；4 次自动计量 POST 仅在浏览器模拟，未到后端。
- 本轮没有真实供应商调用。首轮 20 次实验账本仍已用 19 次；已准备同一第三章、金融底座副本与独立 12 次硬上限的端到端脚本，追加额度尚待用户明确同意。没有重置原账本。新增知识价值、遗漏率、来源忠实度及实际 Token 节省尚未复测，不能宣称已彻底消除语义错误或证明节省比例。

## 2026-09-16 请求上下文治理

已按 [请求上下文治理](UNO-请求上下文治理.md) 实施日志与输入分离：替换旧任务快照、精简可确认重复的失败参数与阅读结果，保护唯一证据、最新工具结果及调用配对。编译和建构不再用原生模型压缩处理这类冗余；完整输入和最终 HTTP 正文均在外发前按 128,000 UTF-8 字节约束。超限明确暂停，恢复建立当前阶段上下文并保留累计请求账本；普通对话不改。

后端 **639/639**、前端 **209/209** 及构建通过，真实 DSH/原生 Kimi SDK 使用合成网络完成三轮验证，无真实模型调用。旧 132,669 展示字符请求回放后为 111,573；完整输入从 224,273 降至 182,339 字节，仍超限而被拦截，不冒充账单节省。独立审查修复了同 ID 独立引用被误去重的风险，并补齐旧快照、原生 replayState、Anthropic 推理与图片二进制的诊断过滤。

正式 **3093 已重启加载**，health 返回 `uno_request_context:1`，专用 preset 不再含 compaction。34 个原生会话空闲，13,635 个知识/草稿/任务/预算文件哈希不变，原书任务仍 ended。完整 App 桌面与 390px 验收通过，实际加载 `index-B44x54ON.js`；提示词统计使用浏览器合成响应，没有 POST 或模型请求。真实模型的质量与经济性尚未复测。


## 2026-09-16 Web 新编译统一入口

用户确认优先修复 Web 仍使用旧流程的问题。之前 `bounded-workflow-v1` 只供 API 显式试用，普通按钮、斜杠命令和省略版本的 API 仍创建旧流程任务，导致已经实现的章节筛选、分阶段权限和请求预算没有作用于日常使用。

现在三个新建入口默认采用同一新编排。页面先核对服务声明的编译版本和当前模型能力，再发送显式版本；后端也为新编译补上默认值，不能因客户端漏字段而回落。历史任务继续沿保存的协议运行，页面明确提示；开始请求先按原始内容核对幂等记录，再应用新建默认值，旧浏览器中未确认的请求可原样核对，不能偷偷改写签名或启动第二个任务。

Kimi Code 继续使用已配置的原生适配器、套餐端点与认证。在 DSH rc.6 缺少可注入网络计数回调的情况下，用限定于该任务异步上下文的 fetch 包装器，在实际发送前持久预留预算；失败、取消、重试均计入，不影响普通对话或历史任务。宿主未安装计数器、计数入口被替换或端点未知时拒绝新任务或外发，不能降级到不计数的路径。原生依赖升级须重跑 `native-kimi-budget.test.mjs`。

已有 Buffer 单元也进入新筛选首包，以真实引用、版本和有限原文样本提供，不重新提取归档书籍、不假装它们是新章节，也不登记全文阅读。未装入的样本不登记为已看到；交付前版本变化使首包失效。Inbox 为空而仍有待处理单元时，界面不再要求用户重新上传。

后端 **608/608**（含本机原生 DSH 专项，无跳过）、前端 **208/208** 测试及 TypeScript/Vite 构建通过。隔离 Edge 页面在 1200×1100 和 390×844 核对新建请求、旧服务拦截、历史恢复及未确认请求原样重试；无真实模型请求。Kimi 专项还复现并修复了“等待凭据时卸载，随后请求绕过计数”的竞态，迟到请求现在被拦截，计数和外发均为零。

确认 34 个原生会话均空闲后，已重启本工作区正式 3093：PID 从 5748 变为 18340。实际 `/api/health` 返回 `uno_compile_entry:2`；`/api/uno/prepare` 返回 `compile_profile:bounded-workflow-v1`、`compile_model.available:true`，金融底座 Inbox 为 0、待处理单元为 13。13,209 个知识、草稿、任务及请求预算文件重启前后哈希完全一致；原任务 `79ee6eef-3b78-4a1c-a7bc-9512242935ca` 仍已结束，没有启动任何编译或聊天模型请求。此处正式加载证据取代下文早期记录中“服务未更新”的状态。

正式 Edge 页面已在 1440×1080、390×844 只读验收：实际加载 `index-CymrHSkD.js`，13 行待处理材料可见；未选材料时开始禁用，勾选后可用，但没有点击开始。无控制台错误、HTTP 错误、错误遮罩或横向溢出。请求全为 GET，未运行模型。截图与请求记录在本机临时目录 `uno-bounded-compile-ui/live-evidence/`。额外发现既有初载问题：实例信息尚未返回时点击入口，`App.tsx` 的 `activeInstanceId` 从 `legacy` 更新后会清空面板；加载完成后的正常入口通过，本次未扩改实例切换逻辑。

当前适配只支持配置中的单一 Web 宿主；不同事件总线可独立使用，但不支持两个宿主在同一事件总线上同时注册 Kimi 计数中间件。新的真实模型质量与经济性尚未对照。旧任务的失败草稿仍保留；草稿替换来源时旧错误来源被合并回来的问题，以及新任务是否复用旧草稿的问题，不属于此次入口修复，不能据此宣称整个编译质量问题已经解决。

撤回时应回退本次入口和模型计数适配的代码及前端构建，然后重启空闲服务；已保存的新任务先明确暂停，不能在同一个任务里改成旧协议。不要用删除版本字段作为兼容开关。

## 2026-09-16 历史对话读取兼容修复

普通对话写入的 `nexo/quick-message` 未登记到原生读取器，因此会话仍在内存时可用，冷启动后却会被拒绝。宿主现在启动时从原生 Loader 导入实际 `dsh-session` 模块，核对 SessionStore 身份后，只登记这一种 UNO 已有处理逻辑的事件。写入与展示共用事件常量，不改写历史文件，也不放行其他未知类型。rc.6 尚无正式的插件事件注册接口；这是针对现有公开目录的兼容适配，升级原生依赖时须重跑原生测试。

读取失败改成单条中文提示，提供重新读取和返回入口；不再把 RPC 堆栈追加到对话。失败保留原会话、草稿和消息流，只有成功读取目标后才切换；并发点击以最后一次选择为准。

后端 **583/583**、前端 **204/204** 测试通过。新增原生 zstd 持久化回归覆盖：旧格式先复现拒绝、登记后冷读、恢复追加、再次冷读、来源及状态保留、无关未知事件仍拒绝，读取前后文件哈希相同。执行时设置 `UNO_DSH_ENTRY` 指向本机 DSH 的 `lib/bin.js`，然后运行 `node --test tests/session-events-native.test.mjs`；未设置时该专项明确跳过。另对用户报错记录的副本验证读取到一条提问与一条回答，原件与副本哈希均未改变；原记录在列表中的删除状态保持不变。

生产构建通过。临时 runtime 的真实 DSH 宿主已成功启动，确认 `/api/health` 的 `uno_session_events: 1`，未复制凭据或真实知识。Edge 页面验收确认：冷历史两条正文可见；不支持的合成记录只显示一个提示，重试不堆叠且不显示原始路径；失败保留原历史和草稿，返回后输入恢复可用；390px 窄屏无横向溢出。

本地 3093 后端重启被自动审批策略阻止，现有进程尚未加载修复。须手动重启 UNO，再确认 `/api/health` 的 `uno_session_events: 1` 后进行实际页面使用。没有继续知识任务或调用聊天模型。

## 2026-09-16 按方向建构与一致性修复

本节记录当时的建构入口与验证状态。新面板接受人的改善方向，程序提出有限候选组，模型局部修订；通过 `direction-driven-v1` 使用实际请求预算、改动分类审核与无修改检查的版本复用。旧任务不迁移；当时编译试验路径仍由 API 显式开启，后续统一入口见本文开头。跨批合并按实际发布收据排除已处理的退役卡，普通写入不能复活退役卡或把系统来源改成文档来源。问题稿、延期、事实核验和无修改结论分别处理。

三组合成固定案例通过真实 Gateway 和检索代码作前后对照，再用相同 DeepSeek Flash、无历史两次批量回答：反例与观点组成功补入原先遗漏的另一方，回答能作具体比较；重复合并组没有明显整体回答优势。追加 **2 次请求、4,677 tokens**，共享首轮累计 **19/20**。预设修订不等于模型自主建构验证；该样本不能推出普遍质量或费用改善比例。

最终后端 **582/582**、前端 **202/202** 测试及生产构建通过；隔离模拟网页在桌面和390px窄屏验证方向必填、版本兼容、模型不可用禁启、候选/未处理范围和实际请求计数。具体契约见 [按方向建构与效果验证](UNO-按方向建构与效果验证.md)。正式服务本轮未重启，8,615个金融底座文件及原PDF哈希一致。临时预算代理已关闭，未继续旧任务。

## 2026-09-16 编译与建构编排重设计

已按 [分块实施方案](UNO-编排重设计与分块实施.md) 接通可选的 `bounded-workflow-v1`：程序提供章级范围、角色证据包、实际外发预算与阶段交接；模型负责投入价值、知识增量、来源忠实度与具体修订。独立审核只读，发现问题最多自动返回作者修订一次，通过对象才交给 Gateway 发布。建构须给出具体卡片与改善目标，新路径暂不写领域层。旧任务与普通 Web 默认入口不自动迁移。

真实同章试验增加 3 次供应商请求，首轮合计 17/20。选材两次读完 7,415 字后，第三次提交因引用省去中文 PDF 换行被拒；阶段预算成功拦截第 4 次外发尝试。0 草稿、0 发布，未进入真实建构；20,583 已报告 tokens 不能与基线草稿交付成本直接比较。根据这一结果修正了原标题和段落预览、排版等价引文定位、小预算配额及暂停状态，失败记录保持原样。最终程序回归与实验详情见分块方案；真实知识质量与端到端经济性尚未通过验收。

最终后端回归 511/511 通过，前端 TypeScript 检查通过；宿主组合测试验证 API、范围、审核、部分发布、人工确认、暂停恢复和局部建构，语义输出由测试预设。隔离 5593 宿主及 5594 预算代理已停止，正式服务未重启。原金融底座 8,615 个文件逐项哈希相同，未新增文件，原 PDF 未变。剩余 3 次请求未消费，没有重置预算或自动恢复任务。

## 2026-09-16 本地语音输入

对话输入框新增本地中文录音转写，文字追加到当前草稿，用户确认后发送。一次最长 60 秒；取消、切换对话、麦克风权限、断连、超时、空识别、迟到结果和资源释放都有对应处理。使用已缓存的 Whisper small CPU int8；不调用聊天模型润色，录音仅在 UNO 宿主内存处理，闲置 5 分钟释放模型。

后端 364 项检查及修正后的分发专项、前端 198 项测试和生产构建通过。5 段中文样本（其中 4 段合成语音）转写 1.66–1.96 秒；数字、否定词和 GDP/ETF 保留，一处“下降到”成“降到”。浏览器从音频采集到真实识别的暖状态全链路约 1.87 秒；正式 3093 已重启加载，冷状态实测 2.86 秒。真实 60 秒上限、取消与跨会话隔离经过验收。设备口音、噪声和手机真机仍不能由这些样本保证，详见 [语音输入与验证](UNO-语音输入.md)。

正式验收额外复现一条旧普通会话读取 500：原生日志加载器不认识已有的 nexo/quick-message 自定义事件。本轮未改写旧日志或放宽原生校验；该兼容问题单独记录；当前新发送的普通对话在以后冷启动时也可能受影响，不能把语音验证通过说成会话恢复已经修复。语音验收空白会话已正常删除，未续跑旧知识任务或调用聊天模型。

## 2026-09-16 Agent 运行契约与上下文交付

已结合历史 152,117 展示字符请求、实际代码和隔离宿主验证重写 [运行机制建议与实施核对](UNO-Agent运行机制修改建议.md)。核心改动：提示与执行按原生会话创建目录绑定知识库；编辑/读卡/批量参数契约一致；错误码与 Gateway 收据保留；模型返回、额度截断和业务完成区分；相同调用连续得到三次相同失败时暂停；正文在登记阅读前按 JSON 字节预算分页；搜索结果收敛元数据；UNO 专用 preset 停用破坏结构的首尾裁剪，保留原生历史压缩。普通对话不增加模型轮次，摘录增加后段同词限制与反例候选，保留真实片段位置。

344 项后端、182 项前端测试和构建通过。最后诊断归属的容错调整另通过 7 项提示词记录测试。原生短材料 17 次、长中文材料 26 次本地模拟请求均完成审核发布且 0 工具错误。长材料最终报文中 120 份工具消息全部可解析，最大 30,130 字节，末尾反例确实进入模型请求。历史 10 组搜索参数只读重放的结果从 74,628 到 57,636 字符（约少 23%），不是同状态模型 A/B，不据此宣称费用或语义质量提升。

阶段工具视图和并行没有启用；仍为 18 个工具、顺序批量。高压力历史压缩使用的代码助手摘要模板尚未完成知识忠实度验证。少数大元数据与辅助输出分支也不在“绝无裁剪”的保证范围内。不同参数交替的空转和成功但没有价值的重复读取仍需实测观察。

验证失误：第一次模拟供应商设置因参数错误被拒绝后，手工流程仍启动了临时合成任务，继承了默认真实模型。发现后已结束；9 个 step 中 8 个报告非缓存输入 17,203、缓存读取 99,584、输出 1,696（其中推理 621，不另加），另 1 个无用量。没有操作正式知识。新增验证驱动先核对独立临时库，再确认设置写入成功和实际模型/端点匹配，失败不创建任务；后续验证均使用本地模拟供应商。这些意外调用不能视为正式质量实验。

正式 UNO 3093 原先没有监听，核对所有知识库没有运行中的业务任务后已启动。`/api/health` 返回本工作区身份及 `uno_runtime_contract:2`；只读 `/api/work` 的当前项均非执行中。运行目录中的 `uno-compile` preset 已更新。启动前后 11,818 份知识 Markdown 总哈希一致。没有继续旧任务、增加预算、迁移或清理知识；DSH 工作区与 3083 服务没有改动。健康版本只证明新宿主加载，原生实际工具链由上述隔离验证佐证；真实模型质量和经济性仍待固定样本对照。

## 2026-09-15 建构中文卡片与进度同步

金融底座建构的 11 次模型请求均返回，但读卡 7 次与编辑 4 次均被英文数字 ID 校验拒绝。已兼容合法中文旧卡 ID，保留任务 ID 和路径校验；状态中的完整授权清单改为分页。网页同步区分超时、网络和后台错误，历史轮询增加超时，避免悬挂请求阻塞后续同步。37 项后端、36 项前端检查与构建通过，隔离浏览器验证错误显示与恢复；正式后台尚未重启，没有重新执行真实任务。截图当时连接失败的具体原因仍缺网络记录。见[核查、修复及生效边界](2026-09-15-建构读卡失败与进度同步核查.md)。

> 2026-09-13。独立重建与架构验证阶段。新编译已改为原生 Agent 两轮处理，程序与隔离原生宿主交互通过；真实模型对复杂材料的理解质量及成本仍待评测。

## 2026-09-15 普通回答被思考额度截断

实测失败请求 `bf90ae5e-d15f-4e61-9cf8-bd626f5f8db7`：DeepSeek V4 Flash、思考开启、low，`max_tokens=4000`；意图判断成功，回答阶段报告输出 4000、其中 reasoningTokens 4000，结束原因 `max-tokens`，没有回答正文。根因是普通问答的固定输出上限没有为思考留余量；同时快速回答只识别旧 `length`，把原生 `max-tokens` 覆盖成笼统失败，提示词记录又把截断记成 completed。

开启显式思考强度的问答请求改用 32768 tokens 共同输出上限，保留用户强度；关闭思考仍为 4000，不增加自动重试或修改全局模型连接。上限不是固定消耗，也不保证任意复杂问题一定完成。核对依据包括实际请求快照和 DeepSeek 官方思考说明 https://api-docs.deepseek.com/guides/thinking_mode/ 。

快速回答保留原生结束原因，区分额度截断、尚无正文、服务错误和中断；供应商原始失败信息不再被笼统替换。提示词列表按 `max-tokens`/`length` 显示“达到输出上限”，旧记录只作显示修正，不改写快照。普通对话的实际失败显示“回答未完成”，历史审计 blocked 状态保持原语义。

后端 336 项、前端 181 项测试与构建通过。覆盖只有思考没有正文、超过旧 4000 输出额度后返回正文、保留 low 强度、关闭思考、HTTP 401 原因、旧快照截断显示与无隐式重试。Edge / Playwright 在 1440×960 和 390×844 验证截断说明与思考用量，无页面错误或写请求；旧服务的快照状态仅在浏览器验收中模拟新版投影，不能证明运行进程已加载。截图位于本机临时目录 `uno-truncation-qa-qjwE29`。

当前正式服务仍是旧进程。核对实例身份和没有运行中的任务后，尝试安全重启，自动审批校验拒绝了停止与重启操作，仅返回 `blocked by policy`；进程没有停止，也没有进行真实模型重试。需用户手动重启 UNO 后使后台额度修复生效。未修改真实卡片或用户模型设置。

## 2026-09-15 最近模型请求查看器

顶栏新增「提示词」，接替旧任务总览的用途；会话内的任务控制保持可用。参考无言 APP 的上下文快照、折叠正文和导出方式，只读检查其源码，没有修改参考项目。列表按当前知识库最近 60 次运行时模型请求倒序，详情显示环节、会话、任务、批次、模型、结果和供应商报告用量。正文可按拼接顺序展开、搜索或导出，原始 JSON 与可读分段共用同一份请求快照。

`llm/stream` 中间件覆盖普通对话、编译/建构的每一步以及压缩、命名等辅助调用。快速问答显式附带会话 ID 与意图/回答阶段；原生编译从真实会话所属任务读取阶段。自有模型适配器在 HTTP 发送前补充实际请求正文，其他 SDK 路线保持运行时快照并说明并非 HTTP 报文、内部重试不单独计数。隐式拼接不从最终回答倒推，规则分段标签也不注入模型。

每个知识库仅保存 30 份请求输入及轻量索引；详情按需读取，分段展开才渲染正文。运行文件不提交、不随纯净发行打包。记录失败不打断模型生成，界面显示保存异常；晚结束的旧请求不会复活已淘汰项。跨库请求 ID、路径穿越和写接口被拒绝，接口沿用宿主同源保护并禁止缓存。连接密钥、HTTP 请求头不进入记录；历史推理、不透明重放状态、图片二进制明确省略，不删改发给模型的真实输入。

验证：后端 333 项、前端 180 项测试与构建通过；最新改动再次通过记录专项。隔离原生宿主在 5493 使用临时合成库与本机模拟供应商完成 17 次模型请求，逐次核对消息正文、工具数量与会话 ID，覆盖选材、阅读制卡和独立审核。Edge / Playwright 在 1440×960 与 390×844 验证入口、展开、正文搜索、无结果、原始 JSON、导出、刷新、重启后读取、Escape 关闭与无横向溢出，无页面/控制台错误，浏览记录没有增加模型请求。Browser plugin not available，使用本机 Playwright；截图和断言位于临时目录 `uno-foundation-native-LGGYBC`。

正式 3093 服务检查时未运行，本轮没有启动正式实例或真实模型任务。前端已构建，下次启动 UNO 后启用记录，功能启用前的请求不补造历史。语义质量和各 SDK 的最终报文不由这次合成验证证明。撤回仅需卸下中间件、接口和界面，不涉及知识迁移。

## 2026-09-14 标签边界与主题筛选补齐

此节补齐上一轮仅统一内容类型标签、遗漏 topics 的范围。存储仍区分内容类型 tags 和主题词 topics；卡片筛选、标签计数、关键词搜索、详情、最近阅读/收藏及图谱比例使用两者的去重合集，Agent 标签检索遵循同一读取规则。保留历史 type 和混存标签，不改写知识文件。图谱默认显示前 12 个标签，可按名称查找或逐批展开；筛选展示不改变全部卡片这一分母。旧后台未提供主题字段时无法仅靠前端补齐，不缓存旧范围的概览来冒充完整统计。

八类定义新增逐标签正文依据门槛和 12 个跨领域边界示例：区分机制与相关性、模型与清单、方法与建议、观察与预测、内容对象与顺带提及。作者与独立审核人直接读取更新后的类型说明，审核提示要求依据不足时修正标签，保留知识正文，不为标签补造内容；争议处理沿用审核 note/issues，不新增分类工作流或知识正文模板。更新开发基线中 tags/topics 混用示例，并标记旧 ontology 只供兼容解释。

后端 321 项、前端 180 项测试及构建通过。隔离合成数据覆盖主题只在元数据出现、跨字段去重、旧卡兼容、正文保持、Agent/目录/详情/图谱一致性，以及大量标签下的展开、查找、无结果、固定分母。Edge / Playwright 在正式前端与替换的只读 API 数据下验证 1440×960、390×844，无页面或控制台错误、无写请求；Browser plugin not available，使用本机 Playwright。只读实际库快照中，“智能体”原文件、目录与图谱均为 13 张（当时全库 211 张，后台仍在处理，数量会变化）。

当前 3093 服务返回的 tag_scope 仍为空，尚未加载本轮后台修改；未重启服务、未批量重分类历史卡片。前端已构建；主题字段与新分类提示需下次启动后台加载。上述程序验证不代表真实模型分类一致性已提升；跨领域示例已准备，尚未进行独立真实模型的分类前后对照。撤回可恢复本轮读取函数、界面和提示文档，无知识迁移需要回滚。

## 2026-09-13 标签浏览与图谱比例

卡片浏览统一按标签筛选，移除单一类型筛选及阅读界面的重复类型展示；列表、最近阅读和收藏显示卡片标签。搜索、领域、关系与排序仍可组合使用；点击正文标签返回对应筛选结果，兼容窄屏阅读。旧卡片的 `type` 仅通过既有兼容读取提供标签，不改写知识文件。

图谱概览以标签替代节点类型分布。每项比例为包含该标签的卡片数除以全部卡片数，同一卡片的重复标签只计一次；多标签覆盖可重叠，总和不要求为 100%。关系分布也显示百分比，概览总量仍显示数量。新后台提供 `node_tags`；当前后台未重启时，前端从卡片目录的真实标签统计补齐，并排除旧类型概览缓存。

后端 320 项、前端 178 项测试及构建通过。使用临时合成卡片，在正式前端与只读 API 响应替换下验证多标签、历史类型兼容、搜索组合、重置、正文标签跳转、旧后台统计回退与图谱百分比；Edge / Playwright 的 1440×960、390×844 截图和交互通过，无页面或控制台错误，无写请求。Browser plugin not available，使用本机 Playwright。前端已构建，可刷新页面体验；未重启后台或改动用户知识。

## 2026-09-13 通用知识工具定位清理

UNO 的领域由用户问题和所选资料决定。界面提示、默认占位示例、普通回答与意图识别提示、编译 Agent 身份、原生 preset、研究报告模板、知识类型总则、README 与现行开发基线已统一这一定位。直接回答与检索按资料需求区分；不再把非某学科的问题自动归为无需检索。明确标注的领域示例、可选专项评测、历史记录和真实知识资料保留，不作为全局身份或领域限制。健康能力名称改为 `general_knowledge_identity`。

后端 319 项、前端 175 项及生产构建通过。新增合成生物学与软件工程资料测试，验证同一检索/回答入口可传递相关正文；意图由模拟模型提供，不代表真实模型语义质量评测。Edge / Playwright 在 `http://127.0.0.1:3093` 验证思考入口、填写问题后按钮启用、取消及新建实例表单的通用示例；1440×960 与 390×844 下无横向溢出、框架错误或控制台错误，未提交对话、创建实例或调用模型。Browser plugin not available，使用本机 Playwright。前端构建已可通过页面刷新读取；正式后台未重启，新模型提示与身份标记需下次启动加载，用户知识文件未改动。

## 2026-09-13 Windows 快捷启动编码修复

`start-nexogenesis.cmd` 使用 Windows PowerShell 5.1，其 `Get-Content` 默认按本机 ANSI 编码读取无 BOM 文件；UTF-8 实例名称“默认知识库”因此被误读，并导致 JSON 解析失败。`prepare-nexogenesis.ps1` 已显式按 UTF-8 读取实例注册表，迁移、快照导出和冒烟测试中的同类 JSON 文件读取也已修正。原实例注册表为合法 UTF-8，未改写配置或知识文件。Windows PowerShell 5.1 下的启动与分发专项 7 项通过，覆盖中文路径、带/不带 BOM、重复准备、注册表原字节保持及真正损坏 JSON 的拒绝；本轮未启动或重启正式服务。

## 2026-09-13 Agent 基础能力优化

编译与建构共用执行层已加入合法 JSON 回执、稳定系统提示与原生动态上下文、局部补丁、逐项批量执行、材料结构与重复块检查、段落定位清理、真实待办恢复及出边依赖发布。短文元数据不再独立成消化单元；模型导航与来源关系分别标记并进入思考，长卡检索补入后段命中证据和边界；重复搜索不再读遍 Buffer 正文来检查缓存。新增本地成本观测。具体边界和调用示例见 [Agent 基础能力改进](UNO-Agent基础能力改进.md)。

后端317项、前端175项、构建通过；隔离原生宿主17次模拟请求使用同一系统前缀、18个工具可见并完成审核发布，0次工具错误。真实费用和知识质量尚需真实样本验证。当前3093服务未更新：自动审批审查拒绝了重启操作，原进程保留。现有1817个知识相关文件哈希一致。

## 2026-09-13 会话控制对齐

聊天、思考和建构已按 DSH 当前源码统一主会话的控制与状态：切换页面不取消后台回答；建构暂停指向真实作者/审核会话并核验退出；原会话可只读讨论、按原预算继续或明确结束。补充要求保存于后端，独立审核结果回到主会话，迟到回执不能抢回页面。另修复快速回答的原生日志保存格式、剩余批次误标完成及窄屏聊天栏裁切。具体行为、验证及保留限制见[会话控制统一](UNO-会话控制统一.md)。源码、构建及临时原生宿主已验证，正式 UNO 后台本轮未重启。

本轮进一步补齐刷新与断网恢复：按知识库恢复本标签页的原会话；断线显示旧状态并暂停控制，重连重新同步；开始回执丢失时使用持久化请求标识，重试只返回原任务。后台进程中断后保留原进度、预算和收据，明确继续才执行。后端 306 项、前端 175 项及构建通过；隔离浏览器覆盖执行中刷新、断网期间编译审核发布、丢失开始回执、实际宿主重启和 390px 布局。没有更新真实服务或重跑真实材料。

## 2026-09-13 整体重构（当前有效）

本机启用“默认知识库”。当日初始化时为空，随后用户已手动放入原始材料；不能沿用初始化时的空库数量判断当前数据。关键版本保存时的清理边界见[版本记录](releases/2026-09-13-uno-knowledge-v3.md)，其中目录数量属于当时快照。

现行协议已升级为 `uno-compile-v3`；编译与建构均按小批使用独立审核上下文，草稿通过后发布。长期偏好、单步开始面板、主题原文、图片归档及多知识库迁移已接通。详细数值、回退和真实验证边界见[整体重构实施记录](UNO-整体重构实施记录.md)。下方 v2 实现说明作为当日较早阶段记录保留，不再代表新版入口。390px 背景工具栏溢出已修复。

## 2026-09-13 大批量导入修复

材料导入使用独立进度面板，不向聊天区追加导入结果。所选文件自动分成每批最多 24 个、正文合计通常不超过 8 MiB 的请求；较大的单文件独立请求，单文件上限 256 MiB，服务端为 multipart 边界额外保留 2 MiB。文件总数量没有 52 个或 1,400 个的限制。

每个文件返回保存、相同内容已存在或失败的结果。单文件失败不撤销其他文件；同名不同内容不覆盖，名称过长时保留扩展名。网络中断或回执缺失计为未确认保存，可只重试未导入项；重试通过名称与内容核对避免重复写入。上传期间界面禁止切库，请求也携带目标库标识，其他窗口切库不能让剩余材料默默进入新库。当前页关闭后不保留待上传 File 对象，需重新选择文件核对。

本轮后端 284 项、前端 167 项测试及构建通过。在隔离的原生宿主与 Edge 浏览器中验证 1,400 个合成小文档通过 59 次请求全部落盘、重复导入、冲突保留原件、保存后丢失响应再重试，以及导入不创建或切换对话。桌面 1440px 与移动端 390px 的结果面板已核验；无页面异常，唯一网络错误来自主动注入的断线。用户报告的历史“52 个”断点缺少当时错误回执，未声称精确复原该次失败。

## 当日较早阶段：v2 入口与契约

编译按钮进入选材、倾向快捷提示词、可编辑要求和预算面板。`/编译 + 提示词` 与 `/compile + 提示词` 保留要求并进入相同选材流程；HTTP 聊天入口有指定材料时创建同一新版任务。倾向选项只添加可见文字，不切换隐藏工作流。

现行设计：[UNO编译功能重构](UNO编译功能重构.md)。[类型与关系说明](design/knowledge-guidance/README.md) 由模型工具与常驻提示直接读取。旧固定摘要批次仅用于历史任务恢复。

## 新编译实际能力

- Python 保守清理控制噪声、PDF 重复页眉页脚及英文断词，按章节或段落回退整理。支持 UTF-8 文本、Markdown、HTML、DOCX 和 PDF；PDF 依赖 PyMuPDF。图像、OCR 和复杂表格提示待核验，保留原件。
- 原文保存于主题 Buffer；同版本共享章节文件，主题保存引用。章节有定位、来源、处理状态和去向，目录与相邻章节可回查。
- 专用 uno-compile preset 装配 11 个工具，使用真实 DSH Agent Loop；模型自主阅读、检索、制卡、改写和合并，取消每批两单元六卡和逐批确认。
- 第一轮理解和制卡；选定章节有处理结果后，第二轮核对命名、多 TAG、摘要、正文、来源、领域和关系。检查针对当前版本，修改后旧检查失效。未解决疑点保存到卡片和检索结果，显示部分完成。
- 模型可修改所有卡片，知识写入通过 HarnessGateway。历史版本保存于 03-Archive/card-history；合并不物理删除旧卡，旧 ID 可读并解析到保留卡，可按历史版本恢复。路径、版本冲突、幂等和原子恢复继续保护文件。
- 新卡为 uno-card-v2，包含稳定 ID、标题、摘要、多个 tags、topics、domains、来源和自然 Markdown 正文。类型八种、关系七种；领域为独立层级索引，不进入卡片清单。领域检索包含子领域和代表卡。
- BM25 索引覆盖卡片和 Buffer 原文，支持数字、中文词与双字片段。支持标签、领域及双向关系查询，索引可从 Markdown 重建。
- 每次请求在 system 提示中重建当前目标、进度、预算和工作摘要，不反复追加旧任务快照。详细规则和正文按需分页。编译直接使用任务标题，省去自动标题生成调用。
- 暂停等待原生回合退出；恢复沿用阶段、成果、调用数和检查点，提取失败可重试。预算约束主循环请求，供应商重试和原生历史压缩可能额外调用，不等于 API 账单硬上限。

该阶段实现入口包括 [知识编辑与检索](../packages/nexogenesis-tools/lib/uno/knowledge.js)、[工具和上下文](../packages/nexogenesis-tools/lib/uno/agent.js)、[工作面板](../web/src/components/UnoKnowledgePanel.tsx)，以及已移除的原生任务桥接 `packages/nexogenesis-web-host/lib/uno-compile.js`；后者仅供 Git 历史追溯。

## 思考及独立建构边界

思考每轮识别意图，直接回应无需资料的问题，或从六条路线集中收集资料后再请求一次回答。处理领域由用户问题与所选资料决定。卡片检索融合原有词面/TAG 匹配、BM25 和一跳关系，另外加入最多 4 个 Buffer 片段；资料包总上限 22,000 字符。稀疏检索不保证召回词面完全不同的类比。

**独立“建构”按钮仍使用固定候选批处理。** 本轮重构新编译及其第二轮整理，不宣称独立建构控制器已改为同样的 Agent；历史关系保留原意。

## 2026-09-13 验证

| 检查 | 实际结果与边界 |
|---|---|
| 后端回归 | 268 项通过，0 失败、0 跳过；新版编译专项 13 项 |
| 前端回归 | 25 个文件、164 项通过 |
| 构建 | TypeScript 与生产构建通过；主 JS 约 566 kB，保留已有体积提示 |
| 原生集成 | 真实 DSH 宿主、preset、工具与 Gateway，使用本地 HTTP 模拟模型；8 次主循环请求完成两轮及归档，每次提供 11 个工具 |
| 浏览器 | Edge / Playwright，1440×960、390×844；按钮、提示词编辑、预算、开始、完成、查看卡片和斜杠入口通过；无相关控制台错误、页面错误或横向溢出 |
| 数据保护 | 历史版本、恢复、合并旧 ID、入链解析、来源并集、冲突、幂等、领域非卡片、修改使旧检查失效均有专项测试 |
| 生命周期 | 模拟事件验证暂停等待 turn/end，不提前释放任务；恢复保留用量与检查点。30 分钟时限未做实际等待测试 |
| 本地服务 | UNO 使用独立 3093 与 .nexogenesis/runtime，能力标记 uno_compile_agent=2。标记只证明代码入口，不证明模型质量 |

Browser plugin not available，按验证技能使用本地 Playwright。最终完整证据位于临时目录 uno-native-v2-u3hxg9，包含 browser-result.json、请求记录及桌面/窄屏截图。当时使用的隔离测试入口 `tools/eval/uno-native-smoke.mjs` 与 `tools/eval/uno-browser-smoke.mjs` 已移除，仅供 Git 历史追溯。未读取真实知识或调用付费模型。

真实宿主验证发现并修复了 preset 服务隔离、工具过滤导致不可见、原生错误信息丢失和额外标题生成调用；模拟单元测试没有替代实际宿主检查。

## 仍需验证

真实模型的章节理解、忠实度、长任务恢复、分类一致性、关系启发与总 Token 成本尚未实测。大知识库索引重建开销、PDF 图像/表格与 OCR、原生历史压缩额外用量需要单独评测。第一轮和第二轮共享同一原生会话，不承诺独立模型审稿；程序质量项不能自动判断史实、观点归属或论证遗漏。

下一步选择几种代表材料，对比核心观点、关键例证、矛盾保留和成本，不先扩张编排平台。

## 2026-09-13 项目关联知识库

“设置 → 思维体表达”新增多选关联知识库，保存到当前项目的 `knowledge_instance_ids`。旧项目未设置时沿用当前库，显式空数组表示不检索知识库。保存后从下一轮生效；运行中的轮次保留开始时的范围。读取已登记知识库，编译写入仍使用当前活动实例。

普通对话按其项目收集选中库的卡片、关系与 Buffer；各库本地排序交错合并，共用 22,000 字符预算，不增加模型轮次。引用带知识库 ID，跨库同名卡片和关系详情可正确打开。移除的库会明确报错，不自动换成其他库。项目关联接口为 `GET/PUT /api/projects/:id/knowledge`，沿用本地请求校验。

本轮后端 269 项、前端 164 项通过，TypeScript 与生产构建通过。隔离原生宿主的浏览器验证覆盖多选、保存、取消、重开、清空、刷新及 1440/390px 设置布局，无相关控制台错误；没有调用真实模型。窄屏设置面板本身不溢出，背景主工具栏存在既有横向溢出（390px 下页面 scrollWidth 715px），本次未改动该工具栏。检索与跨库引用使用合成知识实例验证，不代表真实回答质量评测。验证脚本为 `tests/project-knowledge.test.mjs` 和 `tools/eval/uno-linked-knowledge-browser.mjs`。

撤回本功能可移除入口与项目关联读取逻辑，保留项目字段和知识 Markdown；关联本身不移动或合并知识文件。

本地 3093 服务已在无运行中任务时更新，`project_knowledge_links=1`。真实页面只读核验显示主知识库 72 张、辅助知识库 0 张，默认勾选主库；没有替用户修改关联选择，也没有调用真实模型。

## 保全与撤回

  仅在 D:/UESR/Desktop/NEXOGENESIS-UNO 开发，未改动 DSH 或其他知识库。旧说明见 [2026-09-12 验证记录](history/pre-uno/2026-09-12-UNO-当前实现与验证.md)。停用新版入口和 preset 时保留全部 Markdown、历史版本与收据；历史任务保持原协议。源码、构建和运行进程分别核对。


### 2026-09-13 · 任务暂停与主动结束

- 编译、建构任务面板增加“结束本次任务”，与保留接续能力的“暂停任务”分开。运行中先记录结束意图，禁止新的编译工具操作，等待原生回合退出后显示已结束；重启仍保留结束决定。
- 结束不发布待确认草稿，不删除卡片、草稿、来源或剩余材料，也不将未处理材料计为完成。原任务只能查看；处理剩余材料应新建任务。
- 点击侧栏编译/建构优先打开未结束任务；统一使用主会话显示状态，内部审核会话保留但不重复列出。结束后取消任务置顶。
- 验证：后端 287 项通过，覆盖暂停后结束、执行中等待退出、待确认不误发布和重启保留结束状态；另有前端测试、构建与桌面/390px 浏览器交互验证。浏览器使用隔离的合成库，1,426 份材料哈希前后相同，无模型调用；这不证明真实模型编译质量。


### 2026-09-13 · 编译前置自主筛选

新建编译以 `agent-selection-v1` 从原生 Agent 筛选开始。模型可以浏览全体选材、按需补读、记录消化/排除/暂存，并自主选择下一组；未选中消化的材料不被程序提前清理和排进批次。排除保留原件与筛选依据，免全文阅读和第二次去向审核；正式消化仍保留原有阅读、来源、草稿和审核要求。

界面展示不同去向和理由，筛选排除可查看原件并恢复为待处理。按钮和 `/编译 + 提示词` 共用创建入口。旧任务保持原协议，使用新流程需新建编译；不自动转换或重跑真实材料。详细数值与边界见 [整体实施记录](UNO-整体重构实施记录.md#前置自主筛选补充2026-09-13)。

验证结果：296项后端、169项前端测试及生产构建通过。隔离浏览器验证筛选统计、依据与原件、恢复排除项、结束后统计保留、按钮/斜杠入口和1440/390px布局，无页面异常。合成原生回合覆盖全组排除、混合价值选择、单批暂停接续；没有调用真实模型。3093本地服务已加载 `uno_agent_selection=1`，真实网页选材说明可见，已有任务文件哈希保持一致；未自动筛选或重编用户材料。

</details>
## 2026-09-18：未组织池查看、单卡重编译与删除

卡片目录增加独立“未组织池”入口，读取领域治理正式登记且当前仍为 `domains: []` 的卡片。它汇总跨任务尚未结算的正式池记录，但不把只有空领域字段、从未进入治理池的旧卡自动算入。每张卡显示未组织原因、来源以及入边、出边数量，可阅读全文。

“重新编译并审核”只冻结当前一张卡，使用专用无工具请求读取当前卡及其已绑定来源片段，从空白重写正文并交给独立审核；保持卡片 ID、来源、领域和关系，不启动整书重编译，也不授权新建其他卡片。“删除卡片”要求输入完整卡片 ID，并由 `HarnessGateway.deleteUnassignedCard` 在一个可恢复事务中移除活动 Markdown、清理所有入边及领域代表引用、更新未组织池投影和失效提案。被删原文保存在 `03-Archive/deleted-cards/<id>/<revision>.md`，Journal 与收据记录实际影响。

本入口只管理当前未组织卡。批量建域、领域合并／拆分／改父级／退役仍不在此入口内；删除归档不会自动恢复，需依据收据人工核对后恢复。

验证结果：相关后端 63 项、前端 41 项及生产构建通过。合成实例覆盖池清单、强制单卡复查、独立审核接续、版本冲突、精确 ID 确认、原卡归档、入边与领域导航清理、事务幂等。3093 服务已重启并加载 `uno_unassigned_card_management=1`；Edge / Playwright 检查 1440×1000 与 390×844 的查看、操作入口和删除确认门槛，无控制台错误、HTTP 错误或横向溢出。后续核对发现首版入口把 83 张只有空领域字段、没有治理记录的旧卡误算入池；判定已收紧到正式治理记录。当前实例原任务登记 15 张，其中 6 张已经退出池，剩余 9 张。没有因本次口径修正改写知识卡。
## 2026-09-18：统一停点恢复与无效继续熔断

编译、建构、任务接口、会话工作投影和 Web 面板统一读取 `uno-resume-plan-v1`。可重试失败显示与当前条件对应的动作；领域确认和批次审核进入专用入口；语义修订耗尽显示显眼的处理卡片，要求用户明确放弃未通过候选或延期当前单元；冲突、已完成范围和没有安全路径的停点不再显示通用“从未完成处继续”。服务端在补预算和启动前执行同一判断，前端隐藏按钮不能作为唯一保护。

恢复守卫对比实际语义检查点；如果一次恢复只改变了版本、调用记录或提示文字，又回到同一错误，则记录 `no_progress` 并熔断相同动作。显式处理保留原文、失败审计、已保存卡片和 Gateway 收据。放弃候选只作用于当前单元尚未发布的候选；延期不计全书完成。工作列表的 `can_continue` 使用同一计划，不会再把所有暂停任务都映射成可恢复。

专项验证覆盖恢复计划、单元编译、HTTP 接口、预算无副作用、会话生命周期和前端接口：编译与接口组合回归 75/75，建构、预算与会话恢复 47/47，前端定向 19/19；生产 TypeScript 与 Vite 构建通过。Vite 仍有既有的主包体积警告。

正式 3093 已重启，健康接口返回 `uno_resume_plan=uno-resume-plan-v1` 与 `uno_book_pause_resolution=book-pause-resolution-v1`。当前任务的旧通用 resume 请求返回 409，任务版本与请求预算均不变化。Edge / Playwright 在 1440×1000 与 390×844 下确认显眼停点卡、明确处理按钮、旧通用继续入口消失、无横向溢出及无控制台错误；第二轮验收拦截全部任务 POST，页面导航没有自动发起写请求。Browser plugin not available。

运行核验窗口内，任务记录于 `2026-09-18T15:41:13.189Z` 收到一次真实的 `discard-candidates` 决策，随后把第 16 单元按零卡结算并为第 17 单元新增 3 次模型请求；它在 `COMPILE_RECOVERY_FAILED` 处再次暂停。现有记录能确认专用决策接口被执行，但旧审计字段不能区分具体客户端，不能把该动作归因于服务重启或只读浏览器访问。最终状态为 16/66、version 373、预算 43/160，恢复计划仍为 `decision`，未继续后台执行。

## 2026-09-19：建构侧重与允许调整

知识处理设置保存知识库用途、默认建构侧重、允许操作和组织偏好；建构面板继承默认值，可为本次选择侧重、权限与知识范围。底部持续展示本次任务摘要。具体侧重可组合并选择主要侧重，全部关闭调整权限时只诊断。成果按关系、领域归属和卡片整理展示，只统计正式发布收据。

新任务冻结 `focus-and-permissions-v1`，模型工具、Gateway 草稿写入和正式发布均检查权限。已有领域归属调整要求作者与审核人分别阅读当前领域定义，经独立领域治理事务提交，保留版本、历史和幂等收据。关系调整保持原正文。领域新建、拆分、合并、改定义及卡片新建、拆分尚未接入该入口。完整契约见 [建构侧重与允许调整](UNO-建构侧重与允许调整.md)。

验证：建构、领域治理、发布和偏好组合回归 102/102；前端全量 234/234；TypeScript 与生产构建通过。使用 Browser 插件在隔离合成页面验证 1440×1000 与 390×844 布局、侧重组合、权限配置、只诊断、范围选择、设置继承与旧服务阻断；最终页面无控制台警告或错误，窄屏无横向溢出。预览使用实际 React 组件与模拟接口，不连接真实知识库。

较早一次后端全量记录为 740 项，714 通过、14 失败、12 跳过；失败分别在不含本次功能的初始副本或保留同期编译隔离改动的对照副本复现，因此不声明全量后端通过。最新专项结果不替代该全量限制。证据及选定源码备份位于 `%TEMP%/uno-construction-controls-20260919-004940/`，包括 targeted-final.log、web-final.log、build-final.log、backend-all.log、baseline-failures.log、baseline-concurrent-compile.log 与 ui/ 截图。

本轮没有付费模型请求、真实建构或正式知识写入。3093 后端最后只读核验尚未声明新控制契约，本轮没有重启加载；构建成功与隔离预览不代表正式服务上线，也不证明真实模型的知识发现质量。撤回时仅撤回本功能代码差异与构建，保留同期其他修改及全部知识、任务和收据；已有任务不得改签。
## 2026-09-19：知识处理总分结构与可选逐卡精修

“知识处理”设置页改为“总体安排 → 编译 → 建构 → 对话”的总分结构。总体安排集中知识库用途、组织取向和共同要求；编译集中材料清理、高质量模式和外链图片；建构集中侧重、允许操作和结算方式；对话明确当前只读、按需检索及表达设置的边界，不提供尚未接入执行链路的伪设置。编译开始页可覆盖长期默认质量模式。

新增可选 `refine-each-card-v1`。启用后，单元生成与同名卡处理完成后，每张候选各执行一次无历史精修请求，重新携带本单元原文、本卡与其已有的完整关系端点，允许改进表达并重新确认类型、领域和已有关系；宿主禁止改 ID、来源和新增未完整提供的关系目标。精修后仍执行原 Gateway 预检与独立审核，不把精修响应直接发布。逐卡输入／输出哈希和完成状态保存在单元检查点，后续审核中断后继续不会重复消耗精修请求；局部契约问题在启用隔离时只进入对应卡片待修复项。

验证：高质量编译、恢复、越权端点隔离及编译入口专项 90/90 通过；前端全量 250/250 通过；TypeScript 与生产构建通过，构建只保留既有主包体积提示。后端全量 808 项中 783 通过、13 个既有失败、12 跳过；失败仍集中在凭据夹具、知识质量旧断言、设置连接和既有生命周期测试，本轮新增精修与界面测试均通过，因此不声明后端全量通过。正式 3093 有真实编译任务运行，未重载后端；新版前端只读验收确认总览、锚点跳转、四层信息顺序和高质量成本提示均可见，控制台无警告或错误。当前服务健康信息尚未声明逐卡精修能力，因此不会允许新页面在旧后端上静默启动该模式。测试使用临时合成知识库和模拟模型，没有调用付费模型，也不能据此宣称真实模型精修质量已经得到证明。
