# 全端点冒烟测试报告

> 日期：2026-08-19 02:00
> 目标：http://127.0.0.1:3083（dsh --profile nexogenesis）
> 结果：**PASS 23 / FAIL 0 / TOTAL 23**

| 端点/用例 | 结果 | 详情 |
|---|---|---|
| GET /api/health | PASS | ok=True service=nexogenesis-web-host |
| GET / (index.html) | PASS | status=200 lang=True bytes=11807 |
| fence: Host evil.example -> 403 | PASS | http=403 |
| GET /api/settings | PASS | base_url=https://api.deepseek.com model=deepseek-chat username=用户 has_key=False masked= |
| PUT /api/settings (roundtrip) | PASS | username=冒烟测试 |
| GET /api/projects | PASS | projects=1 first=Nexogenesis 知识库 |
| POST /api/projects | PASS | id=dbc62cd3-a2fe-4920-81d2-7c16ef188d4a name=冒烟测试项目 |
| POST /api/conversations | PASS | id=session-d1c87ac9-d738-4342-911c-bf049184d630 title=新会话 project_id=dbc62cd3-a2fe-4920-81d2-7c16ef188d4a |
| GET /api/conversations/:id | PASS | id=session-d1c87ac9-d738-4342-911c-bf049184d630 messages=0 |
| PATCH /api/conversations/:id (title) | PASS | title=冒烟会话-已改名 |
| GET /api/graph | PASS | nodes=718 edges=2669 firstNode=12国实际资产价格周期规律 |
| GET /api/cards/:id | PASS | id=12国实际资产价格周期规律 title=12 国实际资产价格周期规律：波动排序、幅度增长与日本例外 type=phenomenon bodyLen=1738 |
| GET /api/pipeline/status | PASS | inbox=0 scratch=0 |
| GET /api/pipeline/job | PASS | job= |
| POST /api/pipeline/compile/conversation | PASS | id=session-21b000ea-a4d3-4978-bc23-796dc6ecdf12 task_kind=compile |
| POST /api/candidates/prepare | PASS | prepared=False |
| POST /api/write/confirm (unknown proposal -> 404) | PASS | 404 as designed |
| POST /api/chat (no key -> error or answer) | PASS | http=500 body=
{
  "detail": "turn \u7ED3\u675F\uFF1Allm-deepseek: no API key for provider route \u0022deepseek-official\u0022; store DEEPSEEK_API_KEY through the credentials service (the web Models page writes it), or export DEEPSEEK_API_KEY in the launching environment"
} |
| POST /api/chat/stream (SSE frames) | PASS | status=200 frames=1 sample={"type":"error","detail":"turn 结束：llm-deepseek: no API key for provider route \"deepseek-official\"; store DEEPSEEK_API_ |
| GET /api/events (SSE keep-alive) | PASS | http=200 (curl exit=28; 28=timeout=stream open) |
| POST /api/inbox (multipart) | PASS | response={"saved":["nexo-smoke-upload.md"]} filesCreated=1 |
| POST /api/simulate/:scenario | PASS | body= |
| GET /api/replay/:scenario | PASS | status=200 |

## 说明

- 无 API Key 时 chat 端点预期进入错误路径（HTTP 500 + `turn 结束：llm-deepseek: no API key…`），填入 `DEEPSEEK_API_KEY` 后应转正。
- 测试会话已软删除、Inbox 临时材料已清理、settings 已恢复原值、meta 冒烟记录已清除（项目/会话回到测试前状态）。
- 复跑：`pwsh -File tools/smoke-test.ps1`（前置：产品实例运行在 3083）。

## 基线观察（2026-08-19）

- **图谱边修复（2026-08-19）**：首轮基线发现 718 节点仅 17 边、`relations` 解析为 null——根因是 JS frontmatter 解析器只认缩进列表（`  - item`），而实践仓卡片为顶格列表（`- item`），导致 relations/domains/sources 全部被丢弃。已修复 `graph.js`/`cards.js` 双份解析器（顶格+缩进均支持）：边 17 → 2669，领域索引 29 个，检索扩散恢复。
- **图谱边稀疏已排除复制丢失**：对照实践仓（金融2）确认卡文件逐字节一致、relations 完整（2665 条），问题纯属解析器缺陷，非数据丢失。
- **图谱力导向布局（2026-08-19）**：原 `/api/graph` 用圆形布局（半径 n*22 ≈ 15800px）导致整图缩为圆环、节点不可读。已移植原项目 `runtime/layout.py` 力导向布局（边弹簧 > 排斥 > 向心，600 轮退火，哈希确定性，`.nexogenesis/graph/layout.json` 缓存 + 增量生长）：坐标收敛到 ±280、领域自然聚拢（实测 29 个领域簇分离）。
- **图谱视觉（2026-08-19 用户迭代）**：Obsidian 式节点大小——按连接度缩放（叶子 0.75× → 枢纽 1.4×，实测屏幕尺寸 2.2px → 5.6px），基础半径调大（标准 4.0 / 领域 5.5 / 冲突 4.6），最小屏幕半径随连接度缩放避免钳制抹平差异；领域标签不再常显，所有节点仅鼠标悬浮显示名称胶囊（命中区随节点大小缩放）。
- **meta 缓存修复**：本次测试发现 `meta.js` 进程内缓存从不失效，外部编辑会被旧缓存覆盖（多进程/外部清理场景）。已修复为 mtime/size 失效校验（文件是唯一事实源）。
- **检索工具**：`retrieve`（BM25+图扩散）、`read_card`、`list_domains` 由 agent 会话内调用，本冒烟未驱动模型（无 key）；M2 实测记录见融合设计 §9。
- **preset 挂载**：会话创建显式 `agentPreset: nexogenesis`（`projects.js`），preset 部署于 `$DSH_HOME/.agent-presets/nexogenesis/`。
- **git 基线**：本项目已 `git init`（独立仓库，知识体/node_modules/dist 不入库），本报告与冒烟脚本随基线提交。
