# 2026-08-30 安全修复完成记录

> 状态：已完成专项归档，不作为当前待办清单

  本目录保留本轮原始安全与缺陷审计，便于追溯问题证据、修复要求和验收口径。当前运行事实已回写 `docs/history/pre-uno/2026-08-30-当前实现、运行与限制.md`，工程与文档维护规则已回写 `docs/2026-08-30-编码与脚本规范.md`。

## 修复对应

| 编号 | 完成内容 | 主要回归 |
|---|---|---|
| SEC-01 | 精确同源、短期本地令牌、请求类型约束 | 恶意 Origin、不同端口、缺令牌、`text/plain` |
| SEC-02 | 成熟 YAML 解析与生成、控制字符拒绝、Card 往返深比较 | 换行注入与合法 Unicode 往返 |
| BUG-01 | multipart 字节解析、规范化碰撞与排他写入 | 二进制字节一致、同名不覆盖 |
| SEC-03 | EPUB 条目/大小/总量/倍率上限，Poppler 超时与输出上限 | 小阈值超限、正常材料回归 |
| BUG-02 | 交互先纯校验，后持久化与入队；失败保留 continuation | 错误会话无副作用、入队失败可恢复 |
| SEC-04 | Nexo 会话登记谓词覆盖详情、聊天、取消、事件与认知接口 | 未登记 DSH 会话拒绝 |
| SEC-05 | 路由与存储双层 UUID / 路径约束 | 点段、分隔符与非 UUID 拒绝 |
| DEP-01 | Vite/Vitest 升级、回环开发服务、三套锁文件 | 完整依赖审计 0 项 |
| BUG-03 | SSE 单帧容错、尾帧 flush 与可见警告 | 损坏帧、截断尾帧保留已有内容 |
| RISK-01 | 来源材料数据包络与 compile/digest 明确禁令 | 对抗文字保留为数据且不改变契约 |

## 验证记录

- Node：23 个测试文件全部通过。
- Web：13 个测试文件、83 项测试全部通过。
- Web 生产构建：通过。
- `web/`、`nexogenesis-tools`、`nexogenesis-web-host` 依赖审计：均为 0 项已知漏洞。
- 真实本机 HTTP：合法同源 `200`，恶意来源 `403`，不同回环端口 `403`，纯文本伪装 JSON `415`。

  Poppler 转换已经有有限超时和超时终止，但单次同步转换尚不能响应即时用户取消；这一事实保留在当前限制中，不把它误写为完整异步取消能力。

  本轮没有为了验收而向现有知识体写入测试 Buffer 或 Card，也没有自动消耗用户的模型额度运行完整语义编译、消化和建构。真实 HTTP 请求边界、确定性编译/归档、消化事务、交互恢复和取消路径已分别验证；涉及模型质量与现有知识内容的端到端人工验收继续按近期路线执行。
