{
  "name": "nexogenesis-uno-development",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:web": "npm --prefix web test",
    "build": "npm --prefix web run build"
  },
  "devDependencies": {
    "@deepseek-ai/dsh-llm": "0.1.0-rc.6"
  }
}
