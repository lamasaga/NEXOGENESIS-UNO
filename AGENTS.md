# AGENTS.md — NEXOGENESIS-UNO

> 核对日期：2026-09-21。项目处于独立重建与验证阶段。新建编译以 [单元编译实施](docs/UNO-单元编译实施.md) 为现行契约，领域生命周期以 [领域治理方案](docs/UNO-领域治理方案.md) 为现行契约；事实状态见 [当前实现与验证](docs/UNO-当前实现与验证.md)。
> 入口标识为 `compile_profile=unit-cards-v3`、`workflow=uno-unit-compile-v3`、`compile_review_policy=review-publish-repair-v2`，分类契约为 `ordered-single-type-and-domains-v2`，失败恢复契约为 `compile-response-recovery-v1`，常规审核治理为 `routine-review-governance-v2`。历史 v2 任务按原契约恢复，不改签为 v3；v1 与更早任务只读。新建构以 [策略建构服务](docs/UNO-策略建构服务.md) 为现行契约，[按方向建构与效果验证](docs/UNO-按方向建构与效果验证.md) 仅供历史任务追溯。历史设计与验证结果不能覆盖本文件的现行规则。

## 1. 先确认事实与工作区

- 只在授权的 UNO 工作区或隔离副本实施，不修改 DSH、用户级共享配置或真实知识实例。
- 探索期围绕明确问题做可验证的实现，不让旧控制器默认限定新设计，也不一次性建设未经验证的平台。
- 区分源码实现、程序测试、真实模型效果和实际服务加载。目录、健康标记与构建通过不证明理解质量。
- 开发授权不等于迁移、清理或重编译正式知识库的授权。真实模型实验按已授权材料、副本和预算执行。
- 工作目录、知识库根目录、运行目录及端口分别核对；不启动默认指向旧项目的脚本，不干扰其他服务。

## 2. 不可削弱的知识边界

- Markdown 是唯一语义事实源。可重建索引、任务状态和会话记录不能成为另一套知识事实。
- 所有实质知识写入通过 `HarnessGateway`；Web、Skill、模型工具实现不得绕过 Gateway 直接写卡。
- 保留授权、路径范围、版本冲突、幂等、原子提交或回滚、合法来源与关系端点、历史版本和真实收据。
- 不删除真实卡片；经授权的退役必须保留旧 ID 与去向。普通编译不能复活退役卡片。Profile 修改遵守用户授权边界。
- 保留既有来源和关系原意，不能把来源支持静默改成导航，也不能把模型推断装成原作者论证。`origin: system` 不自动晋升为成熟知识。
- 未读、提取不完整、失败和延期不得算完成。阅读记录证明实际交付区间，收据证明写入事实，两者都不证明语义正确。
- 知识内容、密钥、实例、会话、运行状态与实验产物不进入通用程序提交。

## 3. 编译现行契约（2026-09-17 单元制卡）

- 局部失败隔离采用 `compile-isolation-v1`：新建 v3 启用，已有 v3 在明确继续时启用，历史 v2 不变。通过卡先保存；零歧义归一化、一次局部格式恢复及至多两次问题卡自动修复后仍失败的内容进入未组织池，主线继续后续单元。`quarantined` 不等于完成；原始响应、候选、问题、来源版本和账本保留。待修复候选仍是任务数据，不进入正式卡目录、检索或领域治理；正式无领域卡与待修复内容在同一池界面明确区分。
- 未组织池的单卡／关系／响应／遗漏修复是显式独立操作，每项新处理最多 12 次请求，绑定原任务、单元、快照和幂等请求 ID；不清空原任务计数。经原审核与 Gateway 保存后凭实际收据回填原任务，只有所有待办结算才可完成单元并核验整本归档。待补写关系按修复后的端点重检；版本或来源冲突、认证、额度和用户停止仍暂停，连续三个无可用候选单元熔断。
- 单卡与关系待办使用 `isolated-repair-diagnosis-v1`：新修复任务先由模型根据当前候选、原失败记录和完整关系端点重新诊断；诊断结果替换旧问题，再执行局部修改与独立复核。用户补充只作为保留边界或证据提示，不得转成新的审核错误。肯定关系成立的结论不能进入问题数组或消耗修复次数。
- 已发布但 `domains: []` 的正式卡从未组织池执行重编译时使用 `single-card-source-rewrite-v2`：正文重写、独立审核及必要的局部修复完成后，再用一个只含当前卡和最多 8 个既有领域定义的独立请求核对领域，并由 `HarnessGateway.applyDomainGovernance` 原子挂靠。已有正文无需重写时可直接执行 `single-card-domain-assignment-v1`；该操作只整理领域，不携带来源正文。单卡模型流程不能自动新建领域；无可靠既有领域时保持 partial 和未组织状态。用户可从正式未组织卡显式发起独立人工建域，填写名称、摘要、核心问题、纳入与排除边界并确认后，由 `HarnessGateway.applyDomainGovernance` 在同一事务中创建领域、将当前卡挂为首个成员并关闭未组织项；该操作不调用模型，也不授权后续自动建域。正文已发布但领域请求暂停时保留发布收据，继续任务只恢复领域核对。历史 v1 任务仍按原空领域行为结算。

- 新入口统一为 `compile_profile=unit-cards-v3`、`workflow=uno-unit-compile-v3`。新编译与新建构任务都冻结 `card_classification=ordered-single-type-and-domains-v2`；已在途且未保存该字段的 v3 编译或建构任务继续使用原 `single-type-and-domains-v1`，不在一本材料或一次建构中途混用分类口径。历史 v2 任务保持原分类与恢复行为；v1 和更早任务只读。不改签、迁移或自动重放旧材料。
- Python 负责格式提取、清洗和 Markdown 切分。图书按真实章节，短文章保留整篇，长文章按内部章节与段落；自动判断可由用户明确选择图书/文章覆盖。
- 新任务按创建时所选的精确模型冻结 `workflow_limits=provider-aware-v1`。已登记的百万级上下文模型每个原文单元最多 90,000 Unicode 字符，其余上下文最多 90,000 字符；256K 模型为 72,000/72,000，128K 级模型与能力未知模型保持 60,000/60,000，更小的已登记模型使用更保守上限。原文与其余上下文始终独立计数，不以旧 128 KB 总字节阈值拦截合规中文正文；旧任务没有该快照时继续使用原 60,000/60,000，不在恢复途中改签。
- 宿主一次提交完整原文生成本单元全部卡片，无模型补读工具、无原生多轮工具循环、无历史正文叠加、无子代理笔记。所有外发仍经过既有模型适配器、请求预算和快照记录。
- 生成后由同一 Gateway 预检规格、单一主类型、领域、来源、关系端点与版本；单独的无工具请求检查候选卡内部一致性和关系含义。例行检查不重复携带完整原文；需要来源忠实度或遗漏核验时才使用有界原文请求。
- 输出唯一主类型 `type`，必须按 `conflict/entity/case/concept/method/mechanism/model/claim/phenomenon/undetermined` 顺序判定第一个满足全部最低条件的类型；`phenomenon` 需有观察对象与依据，不作无条件兜底，其他九类均不成立但内容仍完整可复用时选 `undetermined`。同时输出 `domains`（通常 1–2 个、最多 3 个既有领域）；新卡不得输出 `tags/topics`。没有合适领域时允许 `domains: []` 并记录组织债。提示词、Gateway、检索与界面共用该契约及正文骨架。revision 由宿主绑定，模型不接收或生成版本凭据；实际并发变更仍由 Gateway 拒绝。
- `domains: []` 必须进入 `.nexogenesis/domain-governance/state.json` 的可重建未组织池，不能只留在写入 warning。编译在 10 个有效单元、20 张新未组织卡或有足够材料的书末边界触发一次批量检查；每次最多治理 20 张，只提供最多 5 张完整正文，其余为摘要。既有领域挂靠可在 trusted 编译范围内原子执行；新领域默认逐项确认，用户也可在任务开始前明确选择 `domain_approval_mode: automatic`，授权本次编译自动批准通过确定性校验的提案。两种方式都由 `HarnessGateway.applyDomainGovernance` 同时创建领域并挂靠首批成员。
- 领域提案不是正式知识。正式领域记录位于 `01-Cards/_meta/domains/`，必须写清核心问题、纳入边界和排除边界；书名、章节名、作者名、“其他”或“综合”不能作为自动建域结果。领域成员的唯一事实源仍是卡片 `domains`。
- 图片归档清单不等于章节或卡片证据；卡片只展示自身明确引用的图片，无法定位章节的图片只保存在全书目录与原件中。
- 只有问题卡收到局部修复请求，其他卡正文不重写。每个未变的问题范围最多两次自动局部修复；继续前若确定性对账剔除了越权、过期或违反正式契约的审核意见，只为实际发生变化的局部问题重新开放修复额度。恢复不清空请求账本、不重做已成功生成的整个单元，也不对未变问题无限重试。
- 标准质量模式下，正常单元一次生成、一次检查并立即保存通过卡。用户可选 `compile_quality_mode=refine-each-card-v1`：生成与同名卡处理后，每张尚未发布的候选各增加一次独立精修请求，重新提供当前完整原文、本卡与其已有完整关系端点，允许改善表达并复核主类型、既有领域和已有关系；必须保持 ID 与来源，不能新增未完整提供的关系目标，之后仍走原 Gateway 预检和独立审核。精修检查点按卡持久化，继续任务不重复调用。存在具体问题时再增加局部修复及该卡复查请求。返工和复查只含该卡与具体问题，不含原文、参考卡、其他候选或历史；审核问题须包含完成修改需要的简短证据。所有模型响应只输出 JSON，禁止附加修复说明。检查不是全库遍历，也不是多代理独立二审队列。语义检查可能判断错误，模型检查通过不代表知识质量得到证明。
- 检查、复核及启用隔离的生成响应不符合局部 JSON 契约时，先执行无需语义判断的确定性归一化；仍有歧义才允许一次只含失败响应、错误和局部 Schema 的模型恢复。恢复结果必须重新通过原检查器。禁止附带原书或其他卡片上下文、扩大关系端点、把未解决单元算完成、直接写卡或对同一响应反复恢复。无法恢复时按隔离契约留池，历史任务保留原停点行为。详见 [编译失败恢复](docs/UNO-编译失败恢复.md)。
- 参考卡只在本库卡片中确定性匹配：最多 3 张完整卡，另加最多 5 张仅含 ID、标题、主类型、摘要与既有关系的导航摘要，总计不超过 22,000 字符；不访问 Buffer 索引，不声称完成全库语义去重。只有完整交付的旧卡可以被修订或成为新关系依据，摘要参考只用于避重和导航。
- 新建 v3 编译不继承用户的对话思考深度：制卡、遗漏补全与单卡修复使用模型支持的较低有效档，候选审核与单卡复核在可关闭模型上关闭思考；建构使用模型支持的高强度。固定思考且不提供强度档位的模型省略控制参数，保留供应商默认，不能伪造 `off/low/high`。输出预算也随模型能力冻结：能力足够时单元生成最多 98,304 tokens，精修、补全与修复最多 49,152，审核与复核最多 16,384；每项仍受所选模型已登记的单次输出能力上限约束。预算是上限而非固定消耗。达到输出上限时只保留可完整解析的前置卡片，启用隔离的任务留池继续后续单元，其余任务暂停；明确恢复时携带前置卡精简清单请求剩余卡，不覆盖已有候选。历史任务使用已保存快照或原常量恢复，不在途中改变资源契约。
- 新建编译的默认 120 次是一轮可用请求额度。用户明确点击继续时，宿主保留历史消耗和完整账本，并把当前可用额度补足到本轮设定值；重复点击不会在未发生新请求时叠加额度。
- 所有保存经过 HarnessGateway，先保存审查通过的卡，再修复未通过的卡。通过卡指向未发布新卡的关系保留在任务中，目标通过后补写，不能因单张失败阻塞全部正文；每次实际提交仍原子执行；保留来源、版本冲突、幂等、历史版本和回滚。Markdown 仍是唯一语义事实源。原书在整本完成前留在 Inbox，成功后核验不可变归档才移出。
- Python 只清除有结构证据的噪声（控制字符、重复空行、PDF 页眉页脚、EPUB 链接目录）；不能因为论证重复就删除正文。原件、图像保全，OCR/图表无法可靠提取时明确缺口。
- 当前方法和正反例由 docs/design/book-compile-method.md、book-compile-examples.md 同批提供给模型。按知识对象完整性与复用价值决定卡片，不按字数或章数凑产量。

## 4. 建构、对话与知识目录

- 旧编译执行器与工具已物理删除；当前建构实现独立为 `construction-host.js` / `construction-workflow.js`，持久化 `uno-compile-v3` 标识仅在 `mode=construct` 时有效。保留历史读取不等于保留旧执行器，禁止以字段改签恢复旧编译。

- 对外保留编译、建构两个知识工作入口；新对话承担交流与知识库辅助思考，默认只读。
- 新建构使用 `strategy-driven-v2` / `uno-construction-v1`：第一次无工具请求根据用户侧重、允许操作和直接说明制定建构策略与选卡策略；宿主校验并冻结候选、选择、版本和工作包后，使用无历史的专用作者请求、独立审核、至多一次局部修复及 Gateway 提交。`direction-driven-v1` 仅供已创建任务按原协议恢复，不自动改签。领域的新建与成员挂靠只使用独立治理事务；旧建构任务中已保存的 `domain_proposals` 仅保留为延期审计记录，不能写入正式领域。
- 允许新建关系的“发现联系”或“综合整理”启用 `random-focus-semantic-retrieval-v2`：全库与领域范围无需预选卡片。每轮用任务 ID 作为可审计随机种子，从本轮范围内尚未检查的有效卡片中无放回抽取一张焦点卡；指定卡片模式只在指定焦点中抽取。焦点冻结后，通过 `all-cards-integration-candidates-v1` 从全部有效知识卡中按共享来源、显式互指、语义单元、类型兼容和既有路径等信号召回最多五个端点，领域、孤立、知识岛和主图位置都不参与焦点或端点优先级。拓扑只保留为统计和后续独立治理依据。宿主直接冻结最多六张卡的小包，不另发模型策略请求，正常只使用一次关系拟稿和一次独立审核；每轮只修改焦点卡，端点只读，不做对称回写。无候选时不调用模型，也不据此宣称卡片必然独立；失败时同一焦点最多换一组未尝试端点。历史 `incremental-relation-weaving-v1` 任务仍按孤立卡后知识岛的原协议恢复，不自动改签。
- 本次不改变对话检索。无需资料时直接回答；需要依据时按路线收集资料，项目明确关联多个库时可从这些库检索。编译写入始终绑定创建任务的知识库，不随 UI 切库迁移。
- 所有库平等位于 `knowledge-bases/<稳定ID>/`。`01-Cards/` 保存正式卡片，领域为独立 Markdown 索引；原件保存在 `03-Archive/books/<原件哈希>/`，清洗拆分单元、目录和提取图片保存在 `05-Buffer/books/<原件哈希>/<提取哈希>/`。已有 Archive 单元按原引用兼容读取和续跑，不自动迁移。单元是可追溯来源，不能当作临时缓存删除，也不自动加入普通对话的全量检索。Buffer 既有整理原文及稳定索引仍按已有实现检索。
- 卡片采用稳定 ID、唯一主类型、零到三个领域及按类型组织的 Markdown。领域成员以卡片 `domains` 为唯一权威；领域记录中的代表卡不构成成员清单。新编译关系使用 `specialization/supplement/contrast/challenge/analogy/example/application`；`basis=source` 与 `basis=navigation` 明确区分。仅同属领域或词语相近不构成有依据的关系。
- 知识质量看对象完整、来源忠实、边界准确、可独立阅读与关系用途；不以连边率、卡数或自评总分作为完成标准。

## 5. 运行、验证与撤回

- 原生会话仅承载任务归属；新版单元编译由宿主直接调用模型服务，不运行原生工具循环。保留请求计数与上下文预算；模型适配不可计数时阻止新建或外发，不静默降级。
- 输入只含必要规范、本次要求、当前正文、有限成果摘要和待办。不复制全库、全部历史正文或长篇中间笔记，不保存隐藏思维链。
- 取消、暂停、结束和失败返回所属任务。收到停止意图后不得新增知识写入；底层退出前不宣称已经停止，结束状态不能被迟到异常改回可恢复状态。
- UTF-8，Windows 路径使用 LiteralPath 或明确绝对路径；保留用户无关修改。不改第三方协议名来制造“已重建”的表象。
- 程序测试使用临时合成实例；前端需要实际渲染与交互验证。源码、构建、运行服务分别验收。
- 单元编译的程序证据见 [单元编译实施](docs/UNO-单元编译实施.md)：隔离调用链与相关回归通过，没有付费模型请求，不能宣称 v2 语义质量通过。v1 阅读组的真实 Kimi 实验已用 11/12 次授权请求，在隔离复制的 2,669 张金融卡片底座上处理第三章 7,088 字符，新增 4 卡；正式知识未写入，剩余 1 次不使用。该实验不能外推到宿主直提交单元。
- 历史 v1/v2 的测试数、模型实验和正式加载记录只说明对应时点，不能证明 v3。当前源码健康标识另含 `uno_domain_governance=1`、`uno_domain_auto_approval=1` 与 `uno_domain_checkpoint=10-units-20-cards-book-end-v1`；准备接口同时声明 `domain_approval_modes: [manual, automatic]`，防止新版界面在旧服务上静默降级。正式服务是否加载、真实模型能否稳定提出有用领域，都必须单独核验。
- 测试入口为 `npm test`、`npm run test:web`、`npm run build`；专项和证据更新到 [当前实现与验证](docs/UNO-当前实现与验证.md)。
- 单元制卡源码备份为 `%TEMP%/uno-unit-compile`；更早阅读组备份为 `%TEMP%/uno-book-rebuild/before/`，基线哈希在同级 `baseline.json`。撤回只恢复对应一轮程序差异和构建，不覆盖知识库、原书、历史版本、任务或收据；新协议任务先停止，不交给旧控制器继续。
- 文档入口为 [docs/README.md](docs/README.md)。历史记录只用于追溯，旧端口、PID、测试总数与“已加载”声明不能证明新版本已上线。
- 默认分发仍用 `tools/export-clean-agent.ps1`，不携带知识体、凭据、实例、会话、运行状态或实验产物。开发任务提到“编译”不会自行触发真实编译。
